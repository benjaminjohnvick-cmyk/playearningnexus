-- PlayEarning Nexus — PostgreSQL schema (generated from 235 Base44 entities)
-- Generated by tools/gen-schema.mjs. Document-store faithful port: properties live in JSONB "data".
CREATE EXTENSION IF NOT EXISTS pgcrypto;   -- for gen_random_uuid()

-- ABTest: 18 properties
CREATE TABLE IF NOT EXISTS "ABTest" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ABTest_data_gin" ON "ABTest" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ABTest_created" ON "ABTest" (created_date DESC);

-- AIAgentTask: 7 properties
CREATE TABLE IF NOT EXISTS "AIAgentTask" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AIAgentTask_data_gin" ON "AIAgentTask" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AIAgentTask_created" ON "AIAgentTask" (created_date DESC);

-- AIEarningsMonitor: 6 properties
CREATE TABLE IF NOT EXISTS "AIEarningsMonitor" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AIEarningsMonitor_data_gin" ON "AIEarningsMonitor" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AIEarningsMonitor_created" ON "AIEarningsMonitor" (created_date DESC);

-- AIFeedbackAnalysis: 9 properties
CREATE TABLE IF NOT EXISTS "AIFeedbackAnalysis" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AIFeedbackAnalysis_data_gin" ON "AIFeedbackAnalysis" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AIFeedbackAnalysis_created" ON "AIFeedbackAnalysis" (created_date DESC);

-- APIAccessKey: 12 properties
CREATE TABLE IF NOT EXISTS "APIAccessKey" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "APIAccessKey_data_gin" ON "APIAccessKey" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "APIAccessKey_created" ON "APIAccessKey" (created_date DESC);

-- Achievement: 10 properties
CREATE TABLE IF NOT EXISTS "Achievement" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Achievement_data_gin" ON "Achievement" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Achievement_created" ON "Achievement" (created_date DESC);

-- ActivityFeedItem: 7 properties
CREATE TABLE IF NOT EXISTS "ActivityFeedItem" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ActivityFeedItem_data_gin" ON "ActivityFeedItem" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ActivityFeedItem_created" ON "ActivityFeedItem" (created_date DESC);

-- AdAsset: 10 properties
CREATE TABLE IF NOT EXISTS "AdAsset" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdAsset_data_gin" ON "AdAsset" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdAsset_created" ON "AdAsset" (created_date DESC);

-- AdCampaign: 21 properties
CREATE TABLE IF NOT EXISTS "AdCampaign" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdCampaign_data_gin" ON "AdCampaign" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdCampaign_created" ON "AdCampaign" (created_date DESC);

-- AdCreativeTest: 12 properties
CREATE TABLE IF NOT EXISTS "AdCreativeTest" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdCreativeTest_data_gin" ON "AdCreativeTest" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdCreativeTest_created" ON "AdCreativeTest" (created_date DESC);

-- AdLearningMemory: 15 properties
CREATE TABLE IF NOT EXISTS "AdLearningMemory" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdLearningMemory_data_gin" ON "AdLearningMemory" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdLearningMemory_created" ON "AdLearningMemory" (created_date DESC);

-- AdListing: 17 properties
CREATE TABLE IF NOT EXISTS "AdListing" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdListing_data_gin" ON "AdListing" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdListing_created" ON "AdListing" (created_date DESC);

-- AdSchedule: 10 properties
CREATE TABLE IF NOT EXISTS "AdSchedule" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdSchedule_data_gin" ON "AdSchedule" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdSchedule_created" ON "AdSchedule" (created_date DESC);

-- AdTargetingRule: 7 properties
CREATE TABLE IF NOT EXISTS "AdTargetingRule" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdTargetingRule_data_gin" ON "AdTargetingRule" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdTargetingRule_created" ON "AdTargetingRule" (created_date DESC);

-- AdTemplateMarketplace: 17 properties
CREATE TABLE IF NOT EXISTS "AdTemplateMarketplace" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdTemplateMarketplace_data_gin" ON "AdTemplateMarketplace" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdTemplateMarketplace_created" ON "AdTemplateMarketplace" (created_date DESC);

-- AdTemplatePurchase: 7 properties
CREATE TABLE IF NOT EXISTS "AdTemplatePurchase" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdTemplatePurchase_data_gin" ON "AdTemplatePurchase" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdTemplatePurchase_created" ON "AdTemplatePurchase" (created_date DESC);

-- AdTransaction: 10 properties
CREATE TABLE IF NOT EXISTS "AdTransaction" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdTransaction_data_gin" ON "AdTransaction" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdTransaction_created" ON "AdTransaction" (created_date DESC);

-- AdminAuditLog: 7 properties
CREATE TABLE IF NOT EXISTS "AdminAuditLog" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdminAuditLog_data_gin" ON "AdminAuditLog" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdminAuditLog_created" ON "AdminAuditLog" (created_date DESC);

-- AdminCredential: 6 properties
CREATE TABLE IF NOT EXISTS "AdminCredential" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdminCredential_data_gin" ON "AdminCredential" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdminCredential_created" ON "AdminCredential" (created_date DESC);

-- AffiliateAdPost: 12 properties
CREATE TABLE IF NOT EXISTS "AffiliateAdPost" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AffiliateAdPost_data_gin" ON "AffiliateAdPost" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AffiliateAdPost_created" ON "AffiliateAdPost" (created_date DESC);

-- AffiliateChurnPrediction: 13 properties
CREATE TABLE IF NOT EXISTS "AffiliateChurnPrediction" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AffiliateChurnPrediction_data_gin" ON "AffiliateChurnPrediction" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AffiliateChurnPrediction_created" ON "AffiliateChurnPrediction" (created_date DESC);

-- AffiliateContentSchedule: 10 properties
CREATE TABLE IF NOT EXISTS "AffiliateContentSchedule" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AffiliateContentSchedule_data_gin" ON "AffiliateContentSchedule" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AffiliateContentSchedule_created" ON "AffiliateContentSchedule" (created_date DESC);

-- AffiliateDispute: 15 properties
CREATE TABLE IF NOT EXISTS "AffiliateDispute" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AffiliateDispute_data_gin" ON "AffiliateDispute" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AffiliateDispute_created" ON "AffiliateDispute" (created_date DESC);

-- AffiliateGrowthCampaign: 15 properties
CREATE TABLE IF NOT EXISTS "AffiliateGrowthCampaign" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AffiliateGrowthCampaign_data_gin" ON "AffiliateGrowthCampaign" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AffiliateGrowthCampaign_created" ON "AffiliateGrowthCampaign" (created_date DESC);

-- AffiliateOnboarding: 10 properties
CREATE TABLE IF NOT EXISTS "AffiliateOnboarding" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AffiliateOnboarding_data_gin" ON "AffiliateOnboarding" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AffiliateOnboarding_created" ON "AffiliateOnboarding" (created_date DESC);

-- AffiliatePerformanceSnapshot: 8 properties
CREATE TABLE IF NOT EXISTS "AffiliatePerformanceSnapshot" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AffiliatePerformanceSnapshot_data_gin" ON "AffiliatePerformanceSnapshot" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AffiliatePerformanceSnapshot_created" ON "AffiliatePerformanceSnapshot" (created_date DESC);

-- AffiliateProduct: 13 properties
CREATE TABLE IF NOT EXISTS "AffiliateProduct" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AffiliateProduct_data_gin" ON "AffiliateProduct" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AffiliateProduct_created" ON "AffiliateProduct" (created_date DESC);

-- AffiliateSale: 8 properties
CREATE TABLE IF NOT EXISTS "AffiliateSale" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AffiliateSale_data_gin" ON "AffiliateSale" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AffiliateSale_created" ON "AffiliateSale" (created_date DESC);

-- AffiliateTier: 12 properties
CREATE TABLE IF NOT EXISTS "AffiliateTier" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AffiliateTier_data_gin" ON "AffiliateTier" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AffiliateTier_created" ON "AffiliateTier" (created_date DESC);

-- AgentLearningMemory: 16 properties
CREATE TABLE IF NOT EXISTS "AgentLearningMemory" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AgentLearningMemory_data_gin" ON "AgentLearningMemory" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AgentLearningMemory_created" ON "AgentLearningMemory" (created_date DESC);

-- AgentPerformanceLog: 18 properties
CREATE TABLE IF NOT EXISTS "AgentPerformanceLog" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AgentPerformanceLog_data_gin" ON "AgentPerformanceLog" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AgentPerformanceLog_created" ON "AgentPerformanceLog" (created_date DESC);

-- AppStorePrice: 12 properties
CREATE TABLE IF NOT EXISTS "AppStorePrice" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AppStorePrice_data_gin" ON "AppStorePrice" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AppStorePrice_created" ON "AppStorePrice" (created_date DESC);

-- AutomatedPayment: 11 properties
CREATE TABLE IF NOT EXISTS "AutomatedPayment" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AutomatedPayment_data_gin" ON "AutomatedPayment" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AutomatedPayment_created" ON "AutomatedPayment" (created_date DESC);

-- AutomationReview: 14 properties
CREATE TABLE IF NOT EXISTS "AutomationReview" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AutomationReview_data_gin" ON "AutomationReview" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AutomationReview_created" ON "AutomationReview" (created_date DESC);

-- BNPLFamilyMember: 6 properties
CREATE TABLE IF NOT EXISTS "BNPLFamilyMember" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "BNPLFamilyMember_data_gin" ON "BNPLFamilyMember" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "BNPLFamilyMember_created" ON "BNPLFamilyMember" (created_date DESC);

-- Badge: 5 properties
CREATE TABLE IF NOT EXISTS "Badge" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Badge_data_gin" ON "Badge" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Badge_created" ON "Badge" (created_date DESC);

-- BuddyMessage: an encouragement message between paired buddies. ENCOURAGEMENT ONLY — never survey answers
-- (answer-wall enforced). data: { pair_id, from_user_id, to_user_id, kind, text, flagged, day }
CREATE TABLE IF NOT EXISTS "BuddyMessage" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "BuddyMessage_data_gin" ON "BuddyMessage" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "BuddyMessage_created" ON "BuddyMessage" (created_date DESC);

-- BuddyPair: two users paired for accountability/encouragement while earning. data: { user_a, user_b,
-- status, source, unlocked_connect, connect_request_by, connected, created_day }
CREATE TABLE IF NOT EXISTS "BuddyPair" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "BuddyPair_data_gin" ON "BuddyPair" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "BuddyPair_created" ON "BuddyPair" (created_date DESC);

-- BuddyVoiceClip: a voice message between two MUTUALLY-CONNECTED buddies. Transcribed for moderation
-- (answer-wall + scam-guard on the transcript) and retained. data: { pair_id, from_user_id, to_user_id,
-- audio_base64, mime, transcript, day, flagged }
CREATE TABLE IF NOT EXISTS "BuddyVoiceClip" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "BuddyVoiceClip_data_gin" ON "BuddyVoiceClip" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "BuddyVoiceClip_created" ON "BuddyVoiceClip" (created_date DESC);

-- BugReport: 13 properties
CREATE TABLE IF NOT EXISTS "BugReport" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "BugReport_data_gin" ON "BugReport" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "BugReport_created" ON "BugReport" (created_date DESC);

-- BurstSession: a user's "earn on the go" state for a day — progress toward the daily goal across many
-- short bursts, resumable across devices. data: { user_id, day, earned_usd, bursts_completed, pace,
-- last_active_at, last_device }
CREATE TABLE IF NOT EXISTS "BurstSession" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "BurstSession_data_gin" ON "BurstSession" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "BurstSession_created" ON "BurstSession" (created_date DESC);

-- BusinessClient: 20 properties
CREATE TABLE IF NOT EXISTS "BusinessClient" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "BusinessClient_data_gin" ON "BusinessClient" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "BusinessClient_created" ON "BusinessClient" (created_date DESC);

-- CRMAutomation: 8 properties
CREATE TABLE IF NOT EXISTS "CRMAutomation" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CRMAutomation_data_gin" ON "CRMAutomation" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CRMAutomation_created" ON "CRMAutomation" (created_date DESC);

-- CRMLead: 17 properties
CREATE TABLE IF NOT EXISTS "CRMLead" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CRMLead_data_gin" ON "CRMLead" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CRMLead_created" ON "CRMLead" (created_date DESC);

-- CRMLeadConversion: 9 properties
CREATE TABLE IF NOT EXISTS "CRMLeadConversion" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CRMLeadConversion_data_gin" ON "CRMLeadConversion" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CRMLeadConversion_created" ON "CRMLeadConversion" (created_date DESC);

-- CRMOutreach: 13 properties
CREATE TABLE IF NOT EXISTS "CRMOutreach" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CRMOutreach_data_gin" ON "CRMOutreach" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CRMOutreach_created" ON "CRMOutreach" (created_date DESC);

-- CRMProspect: 16 properties
CREATE TABLE IF NOT EXISTS "CRMProspect" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CRMProspect_data_gin" ON "CRMProspect" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CRMProspect_created" ON "CRMProspect" (created_date DESC);

-- CRMSegment: 8 properties
CREATE TABLE IF NOT EXISTS "CRMSegment" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CRMSegment_data_gin" ON "CRMSegment" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CRMSegment_created" ON "CRMSegment" (created_date DESC);

-- ChatMessage: 8 properties
CREATE TABLE IF NOT EXISTS "ChatMessage" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ChatMessage_data_gin" ON "ChatMessage" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ChatMessage_created" ON "ChatMessage" (created_date DESC);

-- CloudSave: 9 properties
CREATE TABLE IF NOT EXISTS "CloudSave" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CloudSave_data_gin" ON "CloudSave" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CloudSave_created" ON "CloudSave" (created_date DESC);

-- CompetitorAlert: 14 properties
CREATE TABLE IF NOT EXISTS "CompetitorAlert" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CompetitorAlert_data_gin" ON "CompetitorAlert" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CompetitorAlert_created" ON "CompetitorAlert" (created_date DESC);

-- CompetitorTrendAnalysis: 13 properties
CREATE TABLE IF NOT EXISTS "CompetitorTrendAnalysis" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CompetitorTrendAnalysis_data_gin" ON "CompetitorTrendAnalysis" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CompetitorTrendAnalysis_created" ON "CompetitorTrendAnalysis" (created_date DESC);

-- ContentLibraryTemplate: 20 properties
CREATE TABLE IF NOT EXISTS "ContentLibraryTemplate" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ContentLibraryTemplate_data_gin" ON "ContentLibraryTemplate" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ContentLibraryTemplate_created" ON "ContentLibraryTemplate" (created_date DESC);

-- ContestParticipation: 14 properties
CREATE TABLE IF NOT EXISTS "ContestParticipation" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ContestParticipation_data_gin" ON "ContestParticipation" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ContestParticipation_created" ON "ContestParticipation" (created_date DESC);

-- ContestPowerUp: 6 properties
CREATE TABLE IF NOT EXISTS "ContestPowerUp" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ContestPowerUp_data_gin" ON "ContestPowerUp" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ContestPowerUp_created" ON "ContestPowerUp" (created_date DESC);

-- ContestVerification: 10 properties
CREATE TABLE IF NOT EXISTS "ContestVerification" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ContestVerification_data_gin" ON "ContestVerification" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ContestVerification_created" ON "ContestVerification" (created_date DESC);

-- CosmeticItem: 9 properties
CREATE TABLE IF NOT EXISTS "CosmeticItem" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CosmeticItem_data_gin" ON "CosmeticItem" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CosmeticItem_created" ON "CosmeticItem" (created_date DESC);

-- CreatorPayout: 12 properties
CREATE TABLE IF NOT EXISTS "CreatorPayout" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CreatorPayout_data_gin" ON "CreatorPayout" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CreatorPayout_created" ON "CreatorPayout" (created_date DESC);

-- CreatorProfile: 13 properties
CREATE TABLE IF NOT EXISTS "CreatorProfile" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CreatorProfile_data_gin" ON "CreatorProfile" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CreatorProfile_created" ON "CreatorProfile" (created_date DESC);

-- CreatorSubscriptionTier: 11 properties
CREATE TABLE IF NOT EXISTS "CreatorSubscriptionTier" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CreatorSubscriptionTier_data_gin" ON "CreatorSubscriptionTier" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CreatorSubscriptionTier_created" ON "CreatorSubscriptionTier" (created_date DESC);

-- CrowdfundingCampaign: 13 properties
CREATE TABLE IF NOT EXISTS "CrowdfundingCampaign" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CrowdfundingCampaign_data_gin" ON "CrowdfundingCampaign" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CrowdfundingCampaign_created" ON "CrowdfundingCampaign" (created_date DESC);

-- CustomReferralLink: 13 properties
CREATE TABLE IF NOT EXISTS "CustomReferralLink" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CustomReferralLink_data_gin" ON "CustomReferralLink" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CustomReferralLink_created" ON "CustomReferralLink" (created_date DESC);

-- CustomSubdomain: 11 properties
CREATE TABLE IF NOT EXISTS "CustomSubdomain" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CustomSubdomain_data_gin" ON "CustomSubdomain" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CustomSubdomain_created" ON "CustomSubdomain" (created_date DESC);

-- DailyAISurvey: 8 properties
CREATE TABLE IF NOT EXISTS "DailyAISurvey" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DailyAISurvey_data_gin" ON "DailyAISurvey" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DailyAISurvey_created" ON "DailyAISurvey" (created_date DESC);

-- DailyChallenge: 11 properties
CREATE TABLE IF NOT EXISTS "DailyChallenge" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DailyChallenge_data_gin" ON "DailyChallenge" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DailyChallenge_created" ON "DailyChallenge" (created_date DESC);

-- DailyEarnings: 6 properties
CREATE TABLE IF NOT EXISTS "DailyEarnings" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DailyEarnings_data_gin" ON "DailyEarnings" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DailyEarnings_created" ON "DailyEarnings" (created_date DESC);

-- DailyFeedbackSurvey: 6 properties
CREATE TABLE IF NOT EXISTS "DailyFeedbackSurvey" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DailyFeedbackSurvey_data_gin" ON "DailyFeedbackSurvey" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DailyFeedbackSurvey_created" ON "DailyFeedbackSurvey" (created_date DESC);

-- DeveloperApplication: 21 properties
CREATE TABLE IF NOT EXISTS "DeveloperApplication" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DeveloperApplication_data_gin" ON "DeveloperApplication" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DeveloperApplication_created" ON "DeveloperApplication" (created_date DESC);

-- DeveloperFollow: 3 properties
CREATE TABLE IF NOT EXISTS "DeveloperFollow" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DeveloperFollow_data_gin" ON "DeveloperFollow" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DeveloperFollow_created" ON "DeveloperFollow" (created_date DESC);

-- DeveloperInstallCost: 15 properties
CREATE TABLE IF NOT EXISTS "DeveloperInstallCost" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DeveloperInstallCost_data_gin" ON "DeveloperInstallCost" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DeveloperInstallCost_created" ON "DeveloperInstallCost" (created_date DESC);

-- DeveloperPayout: 12 properties
CREATE TABLE IF NOT EXISTS "DeveloperPayout" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DeveloperPayout_data_gin" ON "DeveloperPayout" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DeveloperPayout_created" ON "DeveloperPayout" (created_date DESC);

-- DeveloperSupportTicket: 11 properties
CREATE TABLE IF NOT EXISTS "DeveloperSupportTicket" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DeveloperSupportTicket_data_gin" ON "DeveloperSupportTicket" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DeveloperSupportTicket_created" ON "DeveloperSupportTicket" (created_date DESC);

-- DisputeClaim: 17 properties
CREATE TABLE IF NOT EXISTS "DisputeClaim" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DisputeClaim_data_gin" ON "DisputeClaim" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DisputeClaim_created" ON "DisputeClaim" (created_date DESC);

-- DisputeNegotiationChat: 9 properties
CREATE TABLE IF NOT EXISTS "DisputeNegotiationChat" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DisputeNegotiationChat_data_gin" ON "DisputeNegotiationChat" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DisputeNegotiationChat_created" ON "DisputeNegotiationChat" (created_date DESC);

-- DisputeNegotiationSettings: 10 properties
CREATE TABLE IF NOT EXISTS "DisputeNegotiationSettings" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DisputeNegotiationSettings_data_gin" ON "DisputeNegotiationSettings" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DisputeNegotiationSettings_created" ON "DisputeNegotiationSettings" (created_date DESC);

-- DynamicPricing: 10 properties
CREATE TABLE IF NOT EXISTS "DynamicPricing" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DynamicPricing_data_gin" ON "DynamicPricing" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DynamicPricing_created" ON "DynamicPricing" (created_date DESC);

-- EarnBackLedger: one row per month tracking TOTAL premium earn-back discount issued across all members —
-- the global monthly kill-switch counter. data: { month (YYYY-MM), premium_issued_usd }
CREATE TABLE IF NOT EXISTS "EarnBackLedger" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "EarnBackLedger_data_gin" ON "EarnBackLedger" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "EarnBackLedger_created" ON "EarnBackLedger" (created_date DESC);

-- EarnBackPlan: a member's prepay-and-earn-back commitment on ONE item. They prepaid (item + portion) and
-- earn the portion back as a discount via daily surveys. Rebate, not lending. "ownership_pct" is a
-- non-tradeable progress label. data: { user_id, item_title, item_price_usd, chosen_pct, discount_target_usd,
-- portion_prepaid_usd, earned_usd, earned_this_month_usd, minutes_required, minutes_done, is_premium, tier,
-- status (active|paused|completed|abandoned), month, grace_used, last_active_day, ownership_pct, created_at }
CREATE TABLE IF NOT EXISTS "EarnBackPlan" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "EarnBackPlan_data_gin" ON "EarnBackPlan" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "EarnBackPlan_created" ON "EarnBackPlan" (created_date DESC);

-- EcosystemConfig: 10 properties
CREATE TABLE IF NOT EXISTS "EcosystemConfig" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "EcosystemConfig_data_gin" ON "EcosystemConfig" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "EcosystemConfig_created" ON "EcosystemConfig" (created_date DESC);

-- EcosystemRunLog: 9 properties
CREATE TABLE IF NOT EXISTS "EcosystemRunLog" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "EcosystemRunLog_data_gin" ON "EcosystemRunLog" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "EcosystemRunLog_created" ON "EcosystemRunLog" (created_date DESC);

-- EmailMarketingFlow: 18 properties
CREATE TABLE IF NOT EXISTS "EmailMarketingFlow" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "EmailMarketingFlow_data_gin" ON "EmailMarketingFlow" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "EmailMarketingFlow_created" ON "EmailMarketingFlow" (created_date DESC);

-- EmailSequence: 13 properties
CREATE TABLE IF NOT EXISTS "EmailSequence" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "EmailSequence_data_gin" ON "EmailSequence" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "EmailSequence_created" ON "EmailSequence" (created_date DESC);

-- FeatureMockup: 15 properties
CREATE TABLE IF NOT EXISTS "FeatureMockup" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "FeatureMockup_data_gin" ON "FeatureMockup" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "FeatureMockup_created" ON "FeatureMockup" (created_date DESC);

-- FeatureVoteSurvey: 14 properties
CREATE TABLE IF NOT EXISTS "FeatureVoteSurvey" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "FeatureVoteSurvey_data_gin" ON "FeatureVoteSurvey" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "FeatureVoteSurvey_created" ON "FeatureVoteSurvey" (created_date DESC);

-- FeedbackSurveyResponse: 6 properties
CREATE TABLE IF NOT EXISTS "FeedbackSurveyResponse" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "FeedbackSurveyResponse_data_gin" ON "FeedbackSurveyResponse" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "FeedbackSurveyResponse_created" ON "FeedbackSurveyResponse" (created_date DESC);

-- FlaggedResponse: 10 properties
CREATE TABLE IF NOT EXISTS "FlaggedResponse" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "FlaggedResponse_data_gin" ON "FlaggedResponse" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "FlaggedResponse_created" ON "FlaggedResponse" (created_date DESC);

-- ForumPost: 10 properties
CREATE TABLE IF NOT EXISTS "ForumPost" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ForumPost_data_gin" ON "ForumPost" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ForumPost_created" ON "ForumPost" (created_date DESC);

-- FraudReport: 7 properties
CREATE TABLE IF NOT EXISTS "FraudReport" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "FraudReport_data_gin" ON "FraudReport" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "FraudReport_created" ON "FraudReport" (created_date DESC);

-- FriendRequest: 4 properties
CREATE TABLE IF NOT EXISTS "FriendRequest" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "FriendRequest_data_gin" ON "FriendRequest" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "FriendRequest_created" ON "FriendRequest" (created_date DESC);

-- Game: 25 properties
CREATE TABLE IF NOT EXISTS "Game" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Game_data_gin" ON "Game" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Game_created" ON "Game" (created_date DESC);

-- GameEngagement: 9 properties
CREATE TABLE IF NOT EXISTS "GameEngagement" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GameEngagement_data_gin" ON "GameEngagement" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GameEngagement_created" ON "GameEngagement" (created_date DESC);

-- GameGuide: 10 properties
CREATE TABLE IF NOT EXISTS "GameGuide" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GameGuide_data_gin" ON "GameGuide" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GameGuide_created" ON "GameGuide" (created_date DESC);

-- GameRating: 6 properties
CREATE TABLE IF NOT EXISTS "GameRating" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GameRating_data_gin" ON "GameRating" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GameRating_created" ON "GameRating" (created_date DESC);

-- GameReview: 13 properties
CREATE TABLE IF NOT EXISTS "GameReview" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GameReview_data_gin" ON "GameReview" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GameReview_created" ON "GameReview" (created_date DESC);

-- GameVote: 4 properties
CREATE TABLE IF NOT EXISTS "GameVote" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GameVote_data_gin" ON "GameVote" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GameVote_created" ON "GameVote" (created_date DESC);

-- GameVoteSurvey: 9 properties
CREATE TABLE IF NOT EXISTS "GameVoteSurvey" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GameVoteSurvey_data_gin" ON "GameVoteSurvey" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GameVoteSurvey_created" ON "GameVoteSurvey" (created_date DESC);

-- GeneratedImage: 6 properties
CREATE TABLE IF NOT EXISTS "GeneratedImage" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GeneratedImage_data_gin" ON "GeneratedImage" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GeneratedImage_created" ON "GeneratedImage" (created_date DESC);

-- GiftTransaction: 8 properties
CREATE TABLE IF NOT EXISTS "GiftTransaction" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GiftTransaction_data_gin" ON "GiftTransaction" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GiftTransaction_created" ON "GiftTransaction" (created_date DESC);

-- GlobalPrestige: 17 properties
CREATE TABLE IF NOT EXISTS "GlobalPrestige" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GlobalPrestige_data_gin" ON "GlobalPrestige" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GlobalPrestige_created" ON "GlobalPrestige" (created_date DESC);

-- GlobalSettings: 5 properties
CREATE TABLE IF NOT EXISTS "GlobalSettings" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GlobalSettings_data_gin" ON "GlobalSettings" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GlobalSettings_created" ON "GlobalSettings" (created_date DESC);

-- GroupContribution: 5 properties
CREATE TABLE IF NOT EXISTS "GroupContribution" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GroupContribution_data_gin" ON "GroupContribution" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GroupContribution_created" ON "GroupContribution" (created_date DESC);

-- GroupSpendRequest: 10 properties
CREATE TABLE IF NOT EXISTS "GroupSpendRequest" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GroupSpendRequest_data_gin" ON "GroupSpendRequest" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GroupSpendRequest_created" ON "GroupSpendRequest" (created_date DESC);

-- GrowthHeatmapData: 12 properties
CREATE TABLE IF NOT EXISTS "GrowthHeatmapData" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GrowthHeatmapData_data_gin" ON "GrowthHeatmapData" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GrowthHeatmapData_created" ON "GrowthHeatmapData" (created_date DESC);

-- Guild: 16 properties
CREATE TABLE IF NOT EXISTS "Guild" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Guild_data_gin" ON "Guild" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Guild_created" ON "Guild" (created_date DESC);

-- GuildChallenge: 15 properties
CREATE TABLE IF NOT EXISTS "GuildChallenge" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GuildChallenge_data_gin" ON "GuildChallenge" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GuildChallenge_created" ON "GuildChallenge" (created_date DESC);

-- GuildMember: 11 properties
CREATE TABLE IF NOT EXISTS "GuildMember" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GuildMember_data_gin" ON "GuildMember" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GuildMember_created" ON "GuildMember" (created_date DESC);

-- GuildReward: 10 properties
CREATE TABLE IF NOT EXISTS "GuildReward" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GuildReward_data_gin" ON "GuildReward" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GuildReward_created" ON "GuildReward" (created_date DESC);

-- HeadToHeadContest: 12 properties
CREATE TABLE IF NOT EXISTS "HeadToHeadContest" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "HeadToHeadContest_data_gin" ON "HeadToHeadContest" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "HeadToHeadContest_created" ON "HeadToHeadContest" (created_date DESC);

-- IAPAdvertisingCredit: 11 properties
CREATE TABLE IF NOT EXISTS "IAPAdvertisingCredit" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "IAPAdvertisingCredit_data_gin" ON "IAPAdvertisingCredit" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "IAPAdvertisingCredit_created" ON "IAPAdvertisingCredit" (created_date DESC);

-- InAppPurchase: 9 properties
CREATE TABLE IF NOT EXISTS "InAppPurchase" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "InAppPurchase_data_gin" ON "InAppPurchase" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "InAppPurchase_created" ON "InAppPurchase" (created_date DESC);

-- InfluencerDeal: 14 properties
CREATE TABLE IF NOT EXISTS "InfluencerDeal" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "InfluencerDeal_data_gin" ON "InfluencerDeal" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "InfluencerDeal_created" ON "InfluencerDeal" (created_date DESC);

-- IntegrationConfig: 5 properties
CREATE TABLE IF NOT EXISTS "IntegrationConfig" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "IntegrationConfig_data_gin" ON "IntegrationConfig" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "IntegrationConfig_created" ON "IntegrationConfig" (created_date DESC);

-- LeaderboardEntry: 9 properties
CREATE TABLE IF NOT EXISTS "LeaderboardEntry" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "LeaderboardEntry_data_gin" ON "LeaderboardEntry" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "LeaderboardEntry_created" ON "LeaderboardEntry" (created_date DESC);

-- LiveEvent: 16 properties
CREATE TABLE IF NOT EXISTS "LiveEvent" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "LiveEvent_data_gin" ON "LiveEvent" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "LiveEvent_created" ON "LiveEvent" (created_date DESC);

-- LocalizationString: 7 properties
CREATE TABLE IF NOT EXISTS "LocalizationString" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "LocalizationString_data_gin" ON "LocalizationString" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "LocalizationString_created" ON "LocalizationString" (created_date DESC);

-- LockoutSession: 11 properties
CREATE TABLE IF NOT EXISTS "LockoutSession" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "LockoutSession_data_gin" ON "LockoutSession" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "LockoutSession_created" ON "LockoutSession" (created_date DESC);

-- MLMNode: 15 properties
CREATE TABLE IF NOT EXISTS "MLMNode" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "MLMNode_data_gin" ON "MLMNode" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "MLMNode_created" ON "MLMNode" (created_date DESC);

-- MarketResearchReport: 12 properties
CREATE TABLE IF NOT EXISTS "MarketResearchReport" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "MarketResearchReport_data_gin" ON "MarketResearchReport" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "MarketResearchReport_created" ON "MarketResearchReport" (created_date DESC);

-- MarketTrendReport: 9 properties
CREATE TABLE IF NOT EXISTS "MarketTrendReport" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "MarketTrendReport_data_gin" ON "MarketTrendReport" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "MarketTrendReport_created" ON "MarketTrendReport" (created_date DESC);

-- MarketingAsset: 13 properties
CREATE TABLE IF NOT EXISTS "MarketingAsset" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "MarketingAsset_data_gin" ON "MarketingAsset" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "MarketingAsset_created" ON "MarketingAsset" (created_date DESC);

-- MarketingCampaign: 14 properties
CREATE TABLE IF NOT EXISTS "MarketingCampaign" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "MarketingCampaign_data_gin" ON "MarketingCampaign" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "MarketingCampaign_created" ON "MarketingCampaign" (created_date DESC);

-- MarketplaceStore: 8 properties
CREATE TABLE IF NOT EXISTS "MarketplaceStore" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "MarketplaceStore_data_gin" ON "MarketplaceStore" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "MarketplaceStore_created" ON "MarketplaceStore" (created_date DESC);

-- MockupVoteSurvey: 7 properties
CREATE TABLE IF NOT EXISTS "MockupVoteSurvey" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "MockupVoteSurvey_data_gin" ON "MockupVoteSurvey" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "MockupVoteSurvey_created" ON "MockupVoteSurvey" (created_date DESC);

-- MoneyTransfer: 6 properties
CREATE TABLE IF NOT EXISTS "MoneyTransfer" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "MoneyTransfer_data_gin" ON "MoneyTransfer" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "MoneyTransfer_created" ON "MoneyTransfer" (created_date DESC);

-- Notification: 10 properties
CREATE TABLE IF NOT EXISTS "Notification" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Notification_data_gin" ON "Notification" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Notification_created" ON "Notification" (created_date DESC);

-- OnboardingProgress: 10 properties
CREATE TABLE IF NOT EXISTS "OnboardingProgress" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "OnboardingProgress_data_gin" ON "OnboardingProgress" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "OnboardingProgress_created" ON "OnboardingProgress" (created_date DESC);

-- OpsShift: a paid operator's (staff/contractor) recurring coverage window, in UTC hours, for 24/7
-- round-the-clock batch-approval coverage. Operators run the company's own fulfillment; they never touch
-- another user's funds. data: { operator_user_id, operator_name, tz, start_hour_utc, end_hour_utc, days[], active }
CREATE TABLE IF NOT EXISTS "OpsShift" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "OpsShift_data_gin" ON "OpsShift" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "OpsShift_created" ON "OpsShift" (created_date DESC);

-- Order: 26 properties
CREATE TABLE IF NOT EXISTS "Order" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Order_data_gin" ON "Order" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Order_created" ON "Order" (created_date DESC);

-- PPCAbTest: 19 properties
CREATE TABLE IF NOT EXISTS "PPCAbTest" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PPCAbTest_data_gin" ON "PPCAbTest" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PPCAbTest_created" ON "PPCAbTest" (created_date DESC);

-- PPCSession: 9 properties
CREATE TABLE IF NOT EXISTS "PPCSession" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PPCSession_data_gin" ON "PPCSession" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PPCSession_created" ON "PPCSession" (created_date DESC);

-- PPCSurvey: 15 properties
CREATE TABLE IF NOT EXISTS "PPCSurvey" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PPCSurvey_data_gin" ON "PPCSurvey" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PPCSurvey_created" ON "PPCSurvey" (created_date DESC);

-- PPCSurveyResponse: 17 properties
CREATE TABLE IF NOT EXISTS "PPCSurveyResponse" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PPCSurveyResponse_data_gin" ON "PPCSurveyResponse" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PPCSurveyResponse_created" ON "PPCSurveyResponse" (created_date DESC);

-- PPCTransaction: 11 properties
CREATE TABLE IF NOT EXISTS "PPCTransaction" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PPCTransaction_data_gin" ON "PPCTransaction" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PPCTransaction_created" ON "PPCTransaction" (created_date DESC);

-- PPCUserTier: 16 properties
CREATE TABLE IF NOT EXISTS "PPCUserTier" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PPCUserTier_data_gin" ON "PPCUserTier" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PPCUserTier_created" ON "PPCUserTier" (created_date DESC);

-- PartnerTier: 14 properties
CREATE TABLE IF NOT EXISTS "PartnerTier" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PartnerTier_data_gin" ON "PartnerTier" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PartnerTier_created" ON "PartnerTier" (created_date DESC);

-- PayPalAccount: 10 properties
CREATE TABLE IF NOT EXISTS "PayPalAccount" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PayPalAccount_data_gin" ON "PayPalAccount" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PayPalAccount_created" ON "PayPalAccount" (created_date DESC);

-- Payout: 18 properties
CREATE TABLE IF NOT EXISTS "Payout" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Payout_data_gin" ON "Payout" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Payout_created" ON "Payout" (created_date DESC);

-- PayoutPreference: 17 properties
CREATE TABLE IF NOT EXISTS "PayoutPreference" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PayoutPreference_data_gin" ON "PayoutPreference" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PayoutPreference_created" ON "PayoutPreference" (created_date DESC);

-- PayoutRecommendation: 9 properties
CREATE TABLE IF NOT EXISTS "PayoutRecommendation" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PayoutRecommendation_data_gin" ON "PayoutRecommendation" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PayoutRecommendation_created" ON "PayoutRecommendation" (created_date DESC);

-- PayoutRequest: 20 properties
CREATE TABLE IF NOT EXISTS "PayoutRequest" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PayoutRequest_data_gin" ON "PayoutRequest" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PayoutRequest_created" ON "PayoutRequest" (created_date DESC);

-- PendingProduct: 19 properties
CREATE TABLE IF NOT EXISTS "PendingProduct" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PendingProduct_data_gin" ON "PendingProduct" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PendingProduct_created" ON "PendingProduct" (created_date DESC);

-- PerformanceAlert: 7 properties
CREATE TABLE IF NOT EXISTS "PerformanceAlert" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PerformanceAlert_data_gin" ON "PerformanceAlert" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PerformanceAlert_created" ON "PerformanceAlert" (created_date DESC);

-- PersonalizedOffer: 14 properties
CREATE TABLE IF NOT EXISTS "PersonalizedOffer" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PersonalizedOffer_data_gin" ON "PersonalizedOffer" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PersonalizedOffer_created" ON "PersonalizedOffer" (created_date DESC);

-- PlatformCredit: 6 properties
CREATE TABLE IF NOT EXISTS "PlatformCredit" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PlatformCredit_data_gin" ON "PlatformCredit" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PlatformCredit_created" ON "PlatformCredit" (created_date DESC);

-- PremiumMembership: 13 properties
CREATE TABLE IF NOT EXISTS "PremiumMembership" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PremiumMembership_data_gin" ON "PremiumMembership" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PremiumMembership_created" ON "PremiumMembership" (created_date DESC);

-- PremiumPlan: 12 properties
CREATE TABLE IF NOT EXISTS "PremiumPlan" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PremiumPlan_data_gin" ON "PremiumPlan" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PremiumPlan_created" ON "PremiumPlan" (created_date DESC);

-- Product: 11 properties
CREATE TABLE IF NOT EXISTS "Product" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Product_data_gin" ON "Product" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Product_created" ON "Product" (created_date DESC);

-- ProductAnalyticsReport: 10 properties
CREATE TABLE IF NOT EXISTS "ProductAnalyticsReport" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ProductAnalyticsReport_data_gin" ON "ProductAnalyticsReport" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ProductAnalyticsReport_created" ON "ProductAnalyticsReport" (created_date DESC);

-- ProductWebsiteAnalytics: 8 properties
CREATE TABLE IF NOT EXISTS "ProductWebsiteAnalytics" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ProductWebsiteAnalytics_data_gin" ON "ProductWebsiteAnalytics" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ProductWebsiteAnalytics_created" ON "ProductWebsiteAnalytics" (created_date DESC);

-- ProductWishlistItem: 17 properties
CREATE TABLE IF NOT EXISTS "ProductWishlistItem" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ProductWishlistItem_data_gin" ON "ProductWishlistItem" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ProductWishlistItem_created" ON "ProductWishlistItem" (created_date DESC);

-- PromoCode: 10 properties
CREATE TABLE IF NOT EXISTS "PromoCode" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PromoCode_data_gin" ON "PromoCode" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PromoCode_created" ON "PromoCode" (created_date DESC);

-- ProviderUsage: real hosted-inference spend per capability per month, for the self-host advisor. Free-tier
-- and self-hosted calls record nothing (real_spend_usd only). data: { month (YYYY-MM), capability
-- (llm|stt|tts|image), real_spend_usd, calls }
CREATE TABLE IF NOT EXISTS "ProviderUsage" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ProviderUsage_data_gin" ON "ProviderUsage" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ProviderUsage_created" ON "ProviderUsage" (created_date DESC);

-- PushSubscription: 7 properties
CREATE TABLE IF NOT EXISTS "PushSubscription" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PushSubscription_data_gin" ON "PushSubscription" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PushSubscription_created" ON "PushSubscription" (created_date DESC);

-- ReconciliationReport: 15 properties
CREATE TABLE IF NOT EXISTS "ReconciliationReport" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReconciliationReport_data_gin" ON "ReconciliationReport" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReconciliationReport_created" ON "ReconciliationReport" (created_date DESC);

-- RedemptionRecord: 12 properties
CREATE TABLE IF NOT EXISTS "RedemptionRecord" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "RedemptionRecord_data_gin" ON "RedemptionRecord" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "RedemptionRecord_created" ON "RedemptionRecord" (created_date DESC);

-- Referral: 15 properties
CREATE TABLE IF NOT EXISTS "Referral" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Referral_data_gin" ON "Referral" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Referral_created" ON "Referral" (created_date DESC);

-- ReferralAchievement: 9 properties
CREATE TABLE IF NOT EXISTS "ReferralAchievement" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralAchievement_data_gin" ON "ReferralAchievement" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralAchievement_created" ON "ReferralAchievement" (created_date DESC);

-- ReferralAnomalyFlag: 16 properties
CREATE TABLE IF NOT EXISTS "ReferralAnomalyFlag" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralAnomalyFlag_data_gin" ON "ReferralAnomalyFlag" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralAnomalyFlag_created" ON "ReferralAnomalyFlag" (created_date DESC);

-- ReferralAutomation: 7 properties
CREATE TABLE IF NOT EXISTS "ReferralAutomation" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralAutomation_data_gin" ON "ReferralAutomation" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralAutomation_created" ON "ReferralAutomation" (created_date DESC);

-- ReferralBadge: 6 properties
CREATE TABLE IF NOT EXISTS "ReferralBadge" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralBadge_data_gin" ON "ReferralBadge" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralBadge_created" ON "ReferralBadge" (created_date DESC);

-- ReferralCampaign: 19 properties
CREATE TABLE IF NOT EXISTS "ReferralCampaign" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralCampaign_data_gin" ON "ReferralCampaign" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralCampaign_created" ON "ReferralCampaign" (created_date DESC);

-- ReferralContentAsset: 9 properties
CREATE TABLE IF NOT EXISTS "ReferralContentAsset" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralContentAsset_data_gin" ON "ReferralContentAsset" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralContentAsset_created" ON "ReferralContentAsset" (created_date DESC);

-- ReferralContest: 17 properties
CREATE TABLE IF NOT EXISTS "ReferralContest" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralContest_data_gin" ON "ReferralContest" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralContest_created" ON "ReferralContest" (created_date DESC);

-- ReferralEmailLog: 5 properties
CREATE TABLE IF NOT EXISTS "ReferralEmailLog" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralEmailLog_data_gin" ON "ReferralEmailLog" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralEmailLog_created" ON "ReferralEmailLog" (created_date DESC);

-- ReferralEmailSequence: 13 properties
CREATE TABLE IF NOT EXISTS "ReferralEmailSequence" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralEmailSequence_data_gin" ON "ReferralEmailSequence" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralEmailSequence_created" ON "ReferralEmailSequence" (created_date DESC);

-- ReferralFollowUp: 9 properties
CREATE TABLE IF NOT EXISTS "ReferralFollowUp" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralFollowUp_data_gin" ON "ReferralFollowUp" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralFollowUp_created" ON "ReferralFollowUp" (created_date DESC);

-- ReferralJackpot: 27 properties
CREATE TABLE IF NOT EXISTS "ReferralJackpot" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralJackpot_data_gin" ON "ReferralJackpot" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralJackpot_created" ON "ReferralJackpot" (created_date DESC);

-- ReferralLifetimeValue: 10 properties
CREATE TABLE IF NOT EXISTS "ReferralLifetimeValue" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralLifetimeValue_data_gin" ON "ReferralLifetimeValue" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralLifetimeValue_created" ON "ReferralLifetimeValue" (created_date DESC);

-- ReferralMilestone: 8 properties
CREATE TABLE IF NOT EXISTS "ReferralMilestone" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralMilestone_data_gin" ON "ReferralMilestone" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralMilestone_created" ON "ReferralMilestone" (created_date DESC);

-- ReferralPayout: 18 properties
CREATE TABLE IF NOT EXISTS "ReferralPayout" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralPayout_data_gin" ON "ReferralPayout" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralPayout_created" ON "ReferralPayout" (created_date DESC);

-- ReferralPostEntry: 15 properties
CREATE TABLE IF NOT EXISTS "ReferralPostEntry" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralPostEntry_data_gin" ON "ReferralPostEntry" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralPostEntry_created" ON "ReferralPostEntry" (created_date DESC);

-- ReferralPrediction: 10 properties
CREATE TABLE IF NOT EXISTS "ReferralPrediction" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralPrediction_data_gin" ON "ReferralPrediction" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralPrediction_created" ON "ReferralPrediction" (created_date DESC);

-- ReferralRiskFlag: 13 properties
CREATE TABLE IF NOT EXISTS "ReferralRiskFlag" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralRiskFlag_data_gin" ON "ReferralRiskFlag" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralRiskFlag_created" ON "ReferralRiskFlag" (created_date DESC);

-- ReferralSquad: 13 properties
CREATE TABLE IF NOT EXISTS "ReferralSquad" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralSquad_data_gin" ON "ReferralSquad" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralSquad_created" ON "ReferralSquad" (created_date DESC);

-- ReferralTier: 8 properties
CREATE TABLE IF NOT EXISTS "ReferralTier" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralTier_data_gin" ON "ReferralTier" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralTier_created" ON "ReferralTier" (created_date DESC);

-- RespondentProfile: 10 properties
CREATE TABLE IF NOT EXISTS "RespondentProfile" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "RespondentProfile_data_gin" ON "RespondentProfile" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "RespondentProfile_created" ON "RespondentProfile" (created_date DESC);

-- RespondentTrustScore: 9 properties
CREATE TABLE IF NOT EXISTS "RespondentTrustScore" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "RespondentTrustScore_data_gin" ON "RespondentTrustScore" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "RespondentTrustScore_created" ON "RespondentTrustScore" (created_date DESC);

-- RetentionCampaign: 7 properties
CREATE TABLE IF NOT EXISTS "RetentionCampaign" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "RetentionCampaign_data_gin" ON "RetentionCampaign" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "RetentionCampaign_created" ON "RetentionCampaign" (created_date DESC);

-- RetentionRisk: 19 properties
CREATE TABLE IF NOT EXISTS "RetentionRisk" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "RetentionRisk_data_gin" ON "RetentionRisk" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "RetentionRisk_created" ON "RetentionRisk" (created_date DESC);

-- RevenueProduct: 14 properties
CREATE TABLE IF NOT EXISTS "RevenueProduct" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "RevenueProduct_data_gin" ON "RevenueProduct" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "RevenueProduct_created" ON "RevenueProduct" (created_date DESC);

-- RewardPerk: 12 properties
CREATE TABLE IF NOT EXISTS "RewardPerk" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "RewardPerk_data_gin" ON "RewardPerk" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "RewardPerk_created" ON "RewardPerk" (created_date DESC);

-- RewardTier: 8 properties
CREATE TABLE IF NOT EXISTS "RewardTier" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "RewardTier_data_gin" ON "RewardTier" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "RewardTier_created" ON "RewardTier" (created_date DESC);

-- SavedSurveySearch: 10 properties
CREATE TABLE IF NOT EXISTS "SavedSurveySearch" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SavedSurveySearch_data_gin" ON "SavedSurveySearch" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SavedSurveySearch_created" ON "SavedSurveySearch" (created_date DESC);

-- Season: 8 properties
CREATE TABLE IF NOT EXISTS "Season" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Season_data_gin" ON "Season" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Season_created" ON "Season" (created_date DESC);

-- SeasonRank: 12 properties
CREATE TABLE IF NOT EXISTS "SeasonRank" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SeasonRank_data_gin" ON "SeasonRank" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SeasonRank_created" ON "SeasonRank" (created_date DESC);

-- SellerMarketplaceListing: 26 properties
CREATE TABLE IF NOT EXISTS "SellerMarketplaceListing" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SellerMarketplaceListing_data_gin" ON "SellerMarketplaceListing" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SellerMarketplaceListing_created" ON "SellerMarketplaceListing" (created_date DESC);

-- SharedWalletGroup: 11 properties
CREATE TABLE IF NOT EXISTS "SharedWalletGroup" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SharedWalletGroup_data_gin" ON "SharedWalletGroup" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SharedWalletGroup_created" ON "SharedWalletGroup" (created_date DESC);

-- SmartNotificationRule: 12 properties
CREATE TABLE IF NOT EXISTS "SmartNotificationRule" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SmartNotificationRule_data_gin" ON "SmartNotificationRule" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SmartNotificationRule_created" ON "SmartNotificationRule" (created_date DESC);

-- SocialConnection: 4 properties
CREATE TABLE IF NOT EXISTS "SocialConnection" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SocialConnection_data_gin" ON "SocialConnection" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SocialConnection_created" ON "SocialConnection" (created_date DESC);

-- SocialMediaConnection: 12 properties
CREATE TABLE IF NOT EXISTS "SocialMediaConnection" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SocialMediaConnection_data_gin" ON "SocialMediaConnection" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SocialMediaConnection_created" ON "SocialMediaConnection" (created_date DESC);

-- SocialMediaPost: 9 properties
CREATE TABLE IF NOT EXISTS "SocialMediaPost" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SocialMediaPost_data_gin" ON "SocialMediaPost" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SocialMediaPost_created" ON "SocialMediaPost" (created_date DESC);

-- SponsoredContent: 15 properties
CREATE TABLE IF NOT EXISTS "SponsoredContent" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SponsoredContent_data_gin" ON "SponsoredContent" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SponsoredContent_created" ON "SponsoredContent" (created_date DESC);

-- SponsoredListing: 20 properties
CREATE TABLE IF NOT EXISTS "SponsoredListing" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SponsoredListing_data_gin" ON "SponsoredListing" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SponsoredListing_created" ON "SponsoredListing" (created_date DESC);

-- SquadActivityFeed: 7 properties
CREATE TABLE IF NOT EXISTS "SquadActivityFeed" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SquadActivityFeed_data_gin" ON "SquadActivityFeed" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SquadActivityFeed_created" ON "SquadActivityFeed" (created_date DESC);

-- SquadMember: 11 properties
CREATE TABLE IF NOT EXISTS "SquadMember" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SquadMember_data_gin" ON "SquadMember" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SquadMember_created" ON "SquadMember" (created_date DESC);

-- Streak: 6 properties
CREATE TABLE IF NOT EXISTS "Streak" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Streak_data_gin" ON "Streak" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Streak_created" ON "Streak" (created_date DESC);

-- StreamSession: 10 properties
CREATE TABLE IF NOT EXISTS "StreamSession" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "StreamSession_data_gin" ON "StreamSession" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "StreamSession_created" ON "StreamSession" (created_date DESC);

-- StreamerSubscription: 9 properties
CREATE TABLE IF NOT EXISTS "StreamerSubscription" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "StreamerSubscription_data_gin" ON "StreamerSubscription" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "StreamerSubscription_created" ON "StreamerSubscription" (created_date DESC);

-- StreamerTip: 8 properties
CREATE TABLE IF NOT EXISTS "StreamerTip" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "StreamerTip_data_gin" ON "StreamerTip" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "StreamerTip_created" ON "StreamerTip" (created_date DESC);

-- Subscription: 8 properties
CREATE TABLE IF NOT EXISTS "Subscription" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Subscription_data_gin" ON "Subscription" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Subscription_created" ON "Subscription" (created_date DESC);

-- SupportTicket: 13 properties
CREATE TABLE IF NOT EXISTS "SupportTicket" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SupportTicket_data_gin" ON "SupportTicket" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SupportTicket_created" ON "SupportTicket" (created_date DESC);

-- SupportTicketDossier: 14 properties
CREATE TABLE IF NOT EXISTS "SupportTicketDossier" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SupportTicketDossier_data_gin" ON "SupportTicketDossier" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SupportTicketDossier_created" ON "SupportTicketDossier" (created_date DESC);

-- Survey: 10 properties
CREATE TABLE IF NOT EXISTS "Survey" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Survey_data_gin" ON "Survey" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Survey_created" ON "Survey" (created_date DESC);

-- SurveyABTest: 22 properties
CREATE TABLE IF NOT EXISTS "SurveyABTest" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SurveyABTest_data_gin" ON "SurveyABTest" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SurveyABTest_created" ON "SurveyABTest" (created_date DESC);

-- SurveyCollabSession: 8 properties
CREATE TABLE IF NOT EXISTS "SurveyCollabSession" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SurveyCollabSession_data_gin" ON "SurveyCollabSession" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SurveyCollabSession_created" ON "SurveyCollabSession" (created_date DESC);

-- SurveyDispute: 20 properties
CREATE TABLE IF NOT EXISTS "SurveyDispute" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SurveyDispute_data_gin" ON "SurveyDispute" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SurveyDispute_created" ON "SurveyDispute" (created_date DESC);

-- SurveyHeatmapData: 7 properties
CREATE TABLE IF NOT EXISTS "SurveyHeatmapData" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SurveyHeatmapData_data_gin" ON "SurveyHeatmapData" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SurveyHeatmapData_created" ON "SurveyHeatmapData" (created_date DESC);

-- SurveyMarketplaceListing: 16 properties
CREATE TABLE IF NOT EXISTS "SurveyMarketplaceListing" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SurveyMarketplaceListing_data_gin" ON "SurveyMarketplaceListing" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SurveyMarketplaceListing_created" ON "SurveyMarketplaceListing" (created_date DESC);

-- SurveyRecommendation: 13 properties
CREATE TABLE IF NOT EXISTS "SurveyRecommendation" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SurveyRecommendation_data_gin" ON "SurveyRecommendation" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SurveyRecommendation_created" ON "SurveyRecommendation" (created_date DESC);

-- SurveySchedule: 9 properties
CREATE TABLE IF NOT EXISTS "SurveySchedule" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SurveySchedule_data_gin" ON "SurveySchedule" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SurveySchedule_created" ON "SurveySchedule" (created_date DESC);

-- SurveyTemplate: 9 properties
CREATE TABLE IF NOT EXISTS "SurveyTemplate" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SurveyTemplate_data_gin" ON "SurveyTemplate" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SurveyTemplate_created" ON "SurveyTemplate" (created_date DESC);

-- TieredMembership: 9 properties
CREATE TABLE IF NOT EXISTS "TieredMembership" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "TieredMembership_data_gin" ON "TieredMembership" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "TieredMembership_created" ON "TieredMembership" (created_date DESC);

-- Tournament: 24 properties
CREATE TABLE IF NOT EXISTS "Tournament" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Tournament_data_gin" ON "Tournament" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Tournament_created" ON "Tournament" (created_date DESC);

-- TournamentAIInsight: 11 properties
CREATE TABLE IF NOT EXISTS "TournamentAIInsight" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "TournamentAIInsight_data_gin" ON "TournamentAIInsight" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "TournamentAIInsight_created" ON "TournamentAIInsight" (created_date DESC);

-- TournamentChallenge: 14 properties
CREATE TABLE IF NOT EXISTS "TournamentChallenge" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "TournamentChallenge_data_gin" ON "TournamentChallenge" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "TournamentChallenge_created" ON "TournamentChallenge" (created_date DESC);

-- TournamentLeaderboard: 10 properties
CREATE TABLE IF NOT EXISTS "TournamentLeaderboard" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "TournamentLeaderboard_data_gin" ON "TournamentLeaderboard" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "TournamentLeaderboard_created" ON "TournamentLeaderboard" (created_date DESC);

-- TournamentMatch: 17 properties
CREATE TABLE IF NOT EXISTS "TournamentMatch" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "TournamentMatch_data_gin" ON "TournamentMatch" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "TournamentMatch_created" ON "TournamentMatch" (created_date DESC);

-- TournamentParticipant: 15 properties
CREATE TABLE IF NOT EXISTS "TournamentParticipant" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "TournamentParticipant_data_gin" ON "TournamentParticipant" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "TournamentParticipant_created" ON "TournamentParticipant" (created_date DESC);

-- Transaction: 10 properties
CREATE TABLE IF NOT EXISTS "Transaction" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Transaction_data_gin" ON "Transaction" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Transaction_created" ON "Transaction" (created_date DESC);

-- TransferRequest: 4 properties
CREATE TABLE IF NOT EXISTS "TransferRequest" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "TransferRequest_data_gin" ON "TransferRequest" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "TransferRequest_created" ON "TransferRequest" (created_date DESC);

-- UXSessionRecording: 29 properties
CREATE TABLE IF NOT EXISTS "UXSessionRecording" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UXSessionRecording_data_gin" ON "UXSessionRecording" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UXSessionRecording_created" ON "UXSessionRecording" (created_date DESC);

-- User: 20 properties
CREATE TABLE IF NOT EXISTS "User" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  email         text UNIQUE,
  password_hash text,
  role          text NOT NULL DEFAULT 'user',
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "User_data_gin" ON "User" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "User_created" ON "User" (created_date DESC);

-- UserAchievement: 4 properties
CREATE TABLE IF NOT EXISTS "UserAchievement" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UserAchievement_data_gin" ON "UserAchievement" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UserAchievement_created" ON "UserAchievement" (created_date DESC);

-- UserAchievementBadge: 10 properties
CREATE TABLE IF NOT EXISTS "UserAchievementBadge" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UserAchievementBadge_data_gin" ON "UserAchievementBadge" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UserAchievementBadge_created" ON "UserAchievementBadge" (created_date DESC);

-- UserActivity: 5 properties
CREATE TABLE IF NOT EXISTS "UserActivity" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UserActivity_data_gin" ON "UserActivity" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UserActivity_created" ON "UserActivity" (created_date DESC);

-- UserBadge: 8 properties
CREATE TABLE IF NOT EXISTS "UserBadge" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UserBadge_data_gin" ON "UserBadge" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UserBadge_created" ON "UserBadge" (created_date DESC);

-- UserGroup: 8 properties
CREATE TABLE IF NOT EXISTS "UserGroup" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UserGroup_data_gin" ON "UserGroup" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UserGroup_created" ON "UserGroup" (created_date DESC);

-- UserInventory: 7 properties
CREATE TABLE IF NOT EXISTS "UserInventory" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UserInventory_data_gin" ON "UserInventory" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UserInventory_created" ON "UserInventory" (created_date DESC);

-- UserJourneyEvent: 14 properties
CREATE TABLE IF NOT EXISTS "UserJourneyEvent" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UserJourneyEvent_data_gin" ON "UserJourneyEvent" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UserJourneyEvent_created" ON "UserJourneyEvent" (created_date DESC);

-- UserLevel: 13 properties
CREATE TABLE IF NOT EXISTS "UserLevel" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UserLevel_data_gin" ON "UserLevel" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UserLevel_created" ON "UserLevel" (created_date DESC);

-- UserPlatformStats: 7 properties
CREATE TABLE IF NOT EXISTS "UserPlatformStats" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UserPlatformStats_data_gin" ON "UserPlatformStats" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UserPlatformStats_created" ON "UserPlatformStats" (created_date DESC);

-- UserRecommendation: 8 properties
CREATE TABLE IF NOT EXISTS "UserRecommendation" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UserRecommendation_data_gin" ON "UserRecommendation" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UserRecommendation_created" ON "UserRecommendation" (created_date DESC);

-- UserSubscription: 14 properties
CREATE TABLE IF NOT EXISTS "UserSubscription" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UserSubscription_data_gin" ON "UserSubscription" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UserSubscription_created" ON "UserSubscription" (created_date DESC);

-- UserSuggestion: 9 properties
CREATE TABLE IF NOT EXISTS "UserSuggestion" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UserSuggestion_data_gin" ON "UserSuggestion" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UserSuggestion_created" ON "UserSuggestion" (created_date DESC);

-- VirtualCurrency: 5 properties
CREATE TABLE IF NOT EXISTS "VirtualCurrency" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "VirtualCurrency_data_gin" ON "VirtualCurrency" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "VirtualCurrency_created" ON "VirtualCurrency" (created_date DESC);

-- WeeklyEvent: 9 properties
CREATE TABLE IF NOT EXISTS "WeeklyEvent" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "WeeklyEvent_data_gin" ON "WeeklyEvent" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "WeeklyEvent_created" ON "WeeklyEvent" (created_date DESC);

-- WeeklyReferralCampaign: 15 properties
CREATE TABLE IF NOT EXISTS "WeeklyReferralCampaign" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "WeeklyReferralCampaign_data_gin" ON "WeeklyReferralCampaign" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "WeeklyReferralCampaign_created" ON "WeeklyReferralCampaign" (created_date DESC);

-- WhiteLabelLicense: 14 properties
CREATE TABLE IF NOT EXISTS "WhiteLabelLicense" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "WhiteLabelLicense_data_gin" ON "WhiteLabelLicense" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "WhiteLabelLicense_created" ON "WhiteLabelLicense" (created_date DESC);

-- WhiteLabelPartner: 16 properties
CREATE TABLE IF NOT EXISTS "WhiteLabelPartner" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "WhiteLabelPartner_data_gin" ON "WhiteLabelPartner" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "WhiteLabelPartner_created" ON "WhiteLabelPartner" (created_date DESC);

-- WishlistShareReferral: 11 properties
CREATE TABLE IF NOT EXISTS "WishlistShareReferral" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "WishlistShareReferral_data_gin" ON "WishlistShareReferral" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "WishlistShareReferral_created" ON "WishlistShareReferral" (created_date DESC);

-- WithdrawalRequest: 14 properties
CREATE TABLE IF NOT EXISTS "WithdrawalRequest" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "WithdrawalRequest_data_gin" ON "WithdrawalRequest" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "WithdrawalRequest_created" ON "WithdrawalRequest" (created_date DESC);

-- YouTubeEmbedAnalytics: 11 properties
CREATE TABLE IF NOT EXISTS "YouTubeEmbedAnalytics" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "YouTubeEmbedAnalytics_data_gin" ON "YouTubeEmbedAnalytics" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "YouTubeEmbedAnalytics_created" ON "YouTubeEmbedAnalytics" (created_date DESC);

-- YouTubeVideoSession: 17 properties
CREATE TABLE IF NOT EXISTS "YouTubeVideoSession" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "YouTubeVideoSession_data_gin" ON "YouTubeVideoSession" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "YouTubeVideoSession_created" ON "YouTubeVideoSession" (created_date DESC);


-- Auto-update updated_date on row change
CREATE OR REPLACE FUNCTION set_updated_date() RETURNS trigger AS $$
BEGIN NEW.updated_date = now(); RETURN NEW; END; $$ LANGUAGE plpgsql;

-- ============================================================
-- Additive tables for features restored after Base44 removal
-- (agent chat conversations, analytics, in-app logs)
-- ============================================================
CREATE TABLE IF NOT EXISTS "AgentConversation" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AgentConversation_owner" ON "AgentConversation" (created_by, created_date DESC);

CREATE TABLE IF NOT EXISTS "AgentMessage" (
  id              text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date    timestamptz NOT NULL DEFAULT now(),
  updated_date    timestamptz NOT NULL DEFAULT now(),
  created_by      text,
  conversation_id text NOT NULL,
  data            jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AgentMessage_conv" ON "AgentMessage" (conversation_id, created_date ASC);

CREATE TABLE IF NOT EXISTS "AnalyticsEvent" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AnalyticsEvent_created" ON "AnalyticsEvent" (created_date DESC);

CREATE TABLE IF NOT EXISTS "AppLog" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AppLog_created" ON "AppLog" (created_date DESC);

-- ============================================================================
-- Survey-evidence pipeline + domain events (added: agent triggers & evidence)
-- ============================================================================
CREATE TABLE IF NOT EXISTS "SurveySignal" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SurveySignal_data_gin" ON "SurveySignal" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SurveySignal_created" ON "SurveySignal" (created_date DESC);

CREATE TABLE IF NOT EXISTS "SurveyEvidence" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SurveyEvidence_data_gin" ON "SurveyEvidence" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SurveyEvidence_created" ON "SurveyEvidence" (created_date DESC);

CREATE TABLE IF NOT EXISTS "DomainEvent" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DomainEvent_data_gin" ON "DomainEvent" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DomainEvent_created" ON "DomainEvent" (created_date DESC);

-- DeviceToken: native push tokens (FCM/APNs) registered by the mobile app
CREATE TABLE IF NOT EXISTS "DeviceToken" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DeviceToken_data_gin" ON "DeviceToken" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DeviceToken_created" ON "DeviceToken" (created_date DESC);

-- PremiumPPCMembership: a user's 1:1 Premium PPC enrollment (card-on-file + advance ledger)
CREATE TABLE IF NOT EXISTS "PremiumPPCMembership" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PremiumPPCMembership_data_gin" ON "PremiumPPCMembership" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PremiumPPCMembership_created" ON "PremiumPPCMembership" (created_date DESC);

-- PremiumPPCCharge: per-day reconcile ledger (met / charged / failed) for Premium PPC
CREATE TABLE IF NOT EXISTS "PremiumPPCCharge" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PremiumPPCCharge_data_gin" ON "PremiumPPCCharge" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PremiumPPCCharge_created" ON "PremiumPPCCharge" (created_date DESC);

-- ===========================================================================
-- Compliance foundation (Master Plan 0.1–0.5). All are the standard jsonb-doc
-- shape used everywhere else in this schema.
-- ===========================================================================

-- ComplianceFlag: live on/off overrides for risky features (0.1 feature-flag layer)
CREATE TABLE IF NOT EXISTS "ComplianceFlag" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ComplianceFlag_data_gin" ON "ComplianceFlag" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ComplianceFlag_created" ON "ComplianceFlag" (created_date DESC);

-- ConsentRecord: append-only consent & disclosure ledger (0.3 + 0.5 terms acceptance)
CREATE TABLE IF NOT EXISTS "ConsentRecord" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ConsentRecord_data_gin" ON "ConsentRecord" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ConsentRecord_created" ON "ConsentRecord" (created_date DESC);

-- MoneyLedgerEntry: immutable money-movement audit log (0.4)
CREATE TABLE IF NOT EXISTS "MoneyLedgerEntry" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "MoneyLedgerEntry_data_gin" ON "MoneyLedgerEntry" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "MoneyLedgerEntry_created" ON "MoneyLedgerEntry" (created_date DESC);

-- IdempotencyKey: one-shot guard for money operations (0.4)
CREATE TABLE IF NOT EXISTS "IdempotencyKey" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "IdempotencyKey_data_gin" ON "IdempotencyKey" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "IdempotencyKey_created" ON "IdempotencyKey" (created_date DESC);
-- Strict exactly-once: a UNIQUE index on the idempotency key (a duplicate insert now fails at the DB
-- rather than racing through the read-then-write check in sdk/ledger.ts).
CREATE UNIQUE INDEX IF NOT EXISTS "IdempotencyKey_key" ON "IdempotencyKey" ((data->>'key'));

-- AffiliateAccount: per-affiliate ledger (replaces the legacy MLMNode) — balance, bounties, tier
CREATE TABLE IF NOT EXISTS "AffiliateAccount" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AffiliateAccount_data_gin" ON "AffiliateAccount" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AffiliateAccount_created" ON "AffiliateAccount" (created_date DESC);

-- DMCARequest: DMCA takedown notices + counter-notices + their resolutions
CREATE TABLE IF NOT EXISTS "DMCARequest" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DMCARequest_data_gin" ON "DMCARequest" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DMCARequest_created" ON "DMCARequest" (created_date DESC);

-- TaxProfile: per-user W-9 tax info for 1099 reporting. RESTRICTED — contains TIN/SSN; encrypt at
-- rest or hold via your 1099 provider in production. Reports surface only the masked TIN.
CREATE TABLE IF NOT EXISTS "TaxProfile" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "TaxProfile_data_gin" ON "TaxProfile" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "TaxProfile_created" ON "TaxProfile" (created_date DESC);

-- Tenant: a brand running its own rewards/play-to-earn on the platform's rails (rewards-as-a-service,
-- flywheel #2). The default tenant is the operator itself. data: { tenant_id, name, status, revenue_share_pct,
-- saas_fee_usd, contact, created_at }
CREATE TABLE IF NOT EXISTS "Tenant" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Tenant_data_gin" ON "Tenant" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Tenant_created" ON "Tenant" (created_date DESC);

-- AI optimization engine entities (self-learning price/settings optimizer)

-- OptimizationSignal
CREATE TABLE IF NOT EXISTS "OptimizationSignal" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "OptimizationSignal_data_gin" ON "OptimizationSignal" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "OptimizationSignal_created" ON "OptimizationSignal" (created_date DESC);

-- OptimizationRecommendation
CREATE TABLE IF NOT EXISTS "OptimizationRecommendation" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "OptimizationRecommendation_data_gin" ON "OptimizationRecommendation" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "OptimizationRecommendation_created" ON "OptimizationRecommendation" (created_date DESC);

-- OptimizationOutcome
CREATE TABLE IF NOT EXISTS "OptimizationOutcome" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "OptimizationOutcome_data_gin" ON "OptimizationOutcome" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "OptimizationOutcome_created" ON "OptimizationOutcome" (created_date DESC);

-- AILearningState
CREATE TABLE IF NOT EXISTS "AILearningState" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AILearningState_data_gin" ON "AILearningState" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AILearningState_created" ON "AILearningState" (created_date DESC);

-- PricingFeedback
CREATE TABLE IF NOT EXISTS "PricingFeedback" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PricingFeedback_data_gin" ON "PricingFeedback" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PricingFeedback_created" ON "PricingFeedback" (created_date DESC);

-- LeaderboardArchive (weekly leaderboard period snapshots)
CREATE TABLE IF NOT EXISTS "LeaderboardArchive" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "LeaderboardArchive_data_gin" ON "LeaderboardArchive" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "LeaderboardArchive_created" ON "LeaderboardArchive" (created_date DESC);

-- OptimizationExperiment
CREATE TABLE IF NOT EXISTS "OptimizationExperiment" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "OptimizationExperiment_data_gin" ON "OptimizationExperiment" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "OptimizationExperiment_created" ON "OptimizationExperiment" (created_date DESC);

-- SessionRating
CREATE TABLE IF NOT EXISTS "SessionRating" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SessionRating_data_gin" ON "SessionRating" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SessionRating_created" ON "SessionRating" (created_date DESC);

-- UserAIProfile
CREATE TABLE IF NOT EXISTS "UserAIProfile" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UserAIProfile_data_gin" ON "UserAIProfile" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UserAIProfile_created" ON "UserAIProfile" (created_date DESC);

-- SurveyHonestyAnalysis
CREATE TABLE IF NOT EXISTS "SurveyHonestyAnalysis" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SurveyHonestyAnalysis_data_gin" ON "SurveyHonestyAnalysis" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SurveyHonestyAnalysis_created" ON "SurveyHonestyAnalysis" (created_date DESC);

-- MarketplaceListing
CREATE TABLE IF NOT EXISTS "MarketplaceListing" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "MarketplaceListing_data_gin" ON "MarketplaceListing" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "MarketplaceListing_created" ON "MarketplaceListing" (created_date DESC);

-- ── Entities added for self-hosted features (households, layaway, points-boost, live experiments,
--    telemetry/session capture, personalization, and the self-learning catalog assistant memory) ──

CREATE TABLE IF NOT EXISTS "AssistantMemory" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AssistantMemory_data_gin" ON "AssistantMemory" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AssistantMemory_created" ON "AssistantMemory" (created_date DESC);

CREATE TABLE IF NOT EXISTS "Household" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Household_data_gin" ON "Household" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Household_created" ON "Household" (created_date DESC);

CREATE TABLE IF NOT EXISTS "InteractionEvent" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "InteractionEvent_data_gin" ON "InteractionEvent" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "InteractionEvent_created" ON "InteractionEvent" (created_date DESC);

-- ItemSavingsGoal: a user's "bank toward this item" goal. Progress is computed live from the user's
-- Site Cash balance vs the item price; notified at 100% (covered). No cash ever leaves the platform.
CREATE TABLE IF NOT EXISTS "ItemSavingsGoal" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ItemSavingsGoal_data_gin" ON "ItemSavingsGoal" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ItemSavingsGoal_created" ON "ItemSavingsGoal" (created_date DESC);

CREATE TABLE IF NOT EXISTS "Layaway" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Layaway_data_gin" ON "Layaway" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Layaway_created" ON "Layaway" (created_date DESC);

CREATE TABLE IF NOT EXISTS "LiveAssignment" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "LiveAssignment_data_gin" ON "LiveAssignment" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "LiveAssignment_created" ON "LiveAssignment" (created_date DESC);

CREATE TABLE IF NOT EXISTS "LiveExperiment" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "LiveExperiment_data_gin" ON "LiveExperiment" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "LiveExperiment_created" ON "LiveExperiment" (created_date DESC);

CREATE TABLE IF NOT EXISTS "LiveMetricEvent" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "LiveMetricEvent_data_gin" ON "LiveMetricEvent" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "LiveMetricEvent_created" ON "LiveMetricEvent" (created_date DESC);

CREATE TABLE IF NOT EXISTS "PointsBoostLedger" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PointsBoostLedger_data_gin" ON "PointsBoostLedger" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PointsBoostLedger_created" ON "PointsBoostLedger" (created_date DESC);

CREATE TABLE IF NOT EXISTS "SessionCaptureFrame" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SessionCaptureFrame_data_gin" ON "SessionCaptureFrame" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SessionCaptureFrame_created" ON "SessionCaptureFrame" (created_date DESC);

CREATE TABLE IF NOT EXISTS "SessionClose" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SessionClose_data_gin" ON "SessionClose" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SessionClose_created" ON "SessionClose" (created_date DESC);

CREATE TABLE IF NOT EXISTS "UXHeatmapSnapshot" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UXHeatmapSnapshot_data_gin" ON "UXHeatmapSnapshot" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UXHeatmapSnapshot_created" ON "UXHeatmapSnapshot" (created_date DESC);

CREATE TABLE IF NOT EXISTS "UserVariantState" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UserVariantState_data_gin" ON "UserVariantState" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UserVariantState_created" ON "UserVariantState" (created_date DESC);

-- ── Catalog taxonomy, KYC responses, and learning/UX analysis entities (audit fix) ──

CREATE TABLE IF NOT EXISTS "CatalogBrowseNode" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CatalogBrowseNode_data_gin" ON "CatalogBrowseNode" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CatalogBrowseNode_created" ON "CatalogBrowseNode" (created_date DESC);

CREATE TABLE IF NOT EXISTS "CatalogCategory" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CatalogCategory_data_gin" ON "CatalogCategory" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CatalogCategory_created" ON "CatalogCategory" (created_date DESC);

CREATE TABLE IF NOT EXISTS "KYCResponse" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "KYCResponse_data_gin" ON "KYCResponse" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "KYCResponse_created" ON "KYCResponse" (created_date DESC);

CREATE TABLE IF NOT EXISTS "OverheadReport" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "OverheadReport_data_gin" ON "OverheadReport" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "OverheadReport_created" ON "OverheadReport" (created_date DESC);

CREATE TABLE IF NOT EXISTS "UXFinding" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UXFinding_data_gin" ON "UXFinding" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UXFinding_created" ON "UXFinding" (created_date DESC);

-- KycSurveyConfig: singleton holding the active (editable) KYC survey + any pending AI proposal
CREATE TABLE IF NOT EXISTS "KycSurveyConfig" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "KycSurveyConfig_data_gin" ON "KycSurveyConfig" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "KycSurveyConfig_created" ON "KycSurveyConfig" (created_date DESC);


-- AI live-oversight: activity feed + human corrections that feed back into learning

CREATE TABLE IF NOT EXISTS "AIActivityLog" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AIActivityLog_data_gin" ON "AIActivityLog" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AIActivityLog_created" ON "AIActivityLog" (created_date DESC);

CREATE TABLE IF NOT EXISTS "AICorrection" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AICorrection_data_gin" ON "AICorrection" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AICorrection_created" ON "AICorrection" (created_date DESC);

-- KYCRewardGrant: deterministic-id claim record making the one-time KYC reward grant idempotent
CREATE TABLE IF NOT EXISTS "KYCRewardGrant" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "KYCRewardGrant_data_gin" ON "KYCRewardGrant" USING gin (data jsonb_path_ops);


-- PremiumEnrollClaim: deterministic-id single-flight guard so a double-click can't double-grant the up-front points
CREATE TABLE IF NOT EXISTS "PremiumEnrollClaim" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PremiumEnrollClaim_data_gin" ON "PremiumEnrollClaim" USING gin (data jsonb_path_ops);

-- PremiumFinancePlan: a member's no-upfront Premium finance cycle. $1/day is deducted from earned Site Cash
-- toward the membership price; overpayment returns as Site Cash at cycle close; an uncovered price downgrades
-- to free. Pay-as-you-earn from rewards — not lending. data: { user_id, cycle_start, cycle_days, price_usd,
-- daily_usd, deducted_usd, earning_days, month_earnings_usd, covered, qualified, status
-- (active|renewed|downgraded), last_deduct_day, created_at }
CREATE TABLE IF NOT EXISTS "PremiumFinancePlan" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PremiumFinancePlan_data_gin" ON "PremiumFinancePlan" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PremiumFinancePlan_created" ON "PremiumFinancePlan" (created_date DESC);

CREATE TABLE IF NOT EXISTS "LoyaltyLedger" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);

CREATE TABLE IF NOT EXISTS "GroupGoal" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GroupGoal_data_gin" ON "GroupGoal" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GroupGoal_created" ON "GroupGoal" (created_date DESC);

CREATE TABLE IF NOT EXISTS "GroupGoalReward" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GroupGoalReward_data_gin" ON "GroupGoalReward" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GroupGoalReward_created" ON "GroupGoalReward" (created_date DESC);

-- GroupSession: an "earn together" group of a user-chosen size (community/belonging). data: { creator_id,
-- size, members[], status, topic, created_day }
CREATE TABLE IF NOT EXISTS "GroupSession" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GroupSession_data_gin" ON "GroupSession" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GroupSession_created" ON "GroupSession" (created_date DESC);

-- GroupMessage: an encouragement message in a GroupSession. Answer-walled + scam-guarded. Retained for
-- safety/moderation. data: { session_id, from_user_id, kind, text, flagged, day }
CREATE TABLE IF NOT EXISTS "GroupMessage" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GroupMessage_data_gin" ON "GroupMessage" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GroupMessage_created" ON "GroupMessage" (created_date DESC);

CREATE TABLE IF NOT EXISTS "VerifiedSurveyMedia" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "VerifiedSurveyMedia_data_gin" ON "VerifiedSurveyMedia" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "VerifiedSurveyMedia_created" ON "VerifiedSurveyMedia" (created_date DESC);

CREATE TABLE IF NOT EXISTS "RevenueEvent" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "RevenueEvent_data_gin" ON "RevenueEvent" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "RevenueEvent_created" ON "RevenueEvent" (created_date DESC);

-- Expense: recorded business expenses (marketing / infra / other) for the growth-budget engine.
CREATE TABLE IF NOT EXISTS "Expense" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Expense_data_gin" ON "Expense" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Expense_created" ON "Expense" (created_date DESC);

-- GrowthPlan: daily snapshots from growthBudgetAutoPlan (reserve, surplus, reinvest budget, projections).
CREATE TABLE IF NOT EXISTS "GrowthPlan" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GrowthPlan_data_gin" ON "GrowthPlan" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GrowthPlan_created" ON "GrowthPlan" (created_date DESC);

-- AdGridAd
CREATE TABLE IF NOT EXISTS "AdGridAd" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdGridAd_data_gin" ON "AdGridAd" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdGridAd_created" ON "AdGridAd" (created_date DESC);

-- AdImpression: a served ad impression (e.g. the 30s survey interstitial). Feeds your own ad-revenue
-- reporting (flywheel #1). data: { user_id, ad_id, placement, seconds, day }
CREATE TABLE IF NOT EXISTS "AdImpression" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdImpression_data_gin" ON "AdImpression" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdImpression_created" ON "AdImpression" (created_date DESC);

-- AffiliatePurchase: a purchase seen through the OPT-IN shopping extension (Honey-style), recorded ONLY
-- after explicit consent (ConsentRecord kind "shopping_tracking"). DATA-MINIMIZED on purpose: merchant,
-- order total, affiliate commission, coarse day/ref — never full carts, item lists, card data, or browsing
-- history. Cashback is closed-loop Site Cash. data: { user_id, merchant, order_total_usd, currency,
-- commission_usd, cashback_usd, network, order_ref, day, consent_kind }
CREATE TABLE IF NOT EXISTS "AffiliatePurchase" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AffiliatePurchase_data_gin" ON "AffiliatePurchase" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AffiliatePurchase_created" ON "AffiliatePurchase" (created_date DESC);

-- FoundingAdvertiser: a founding-advertiser package purchase (advertiser-funded launch). This records an
-- ADVERTISING + membership purchase, NOT an investment — there is no guaranteed return. Funds are held in
-- escrow until the premium-user launch milestone; if it's missed by the deadline, status flips to
-- refund_due. "Value back" is variable member survey earnings (separate, not an offset). data: {
--   user_id, tier, price_usd, term_years, status (escrowed|active|refund_due|refunded|cancelled),
--   impressions_per_year, impressions_served, purchased_at, disclosures_version, milestone_met }
CREATE TABLE IF NOT EXISTS "FoundingAdvertiser" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "FoundingAdvertiser_data_gin" ON "FoundingAdvertiser" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "FoundingAdvertiser_created" ON "FoundingAdvertiser" (created_date DESC);

-- EarnedAdvertiser: the FREE "earn-to-unlock" advertiser tier AND the no-upfront (participation-term) Tier 1
-- option. The member pays NOTHING and owes NOTHING; the platform generates its value by monetizing the
-- member's ongoing activity over time (an LTV target), never as a fee/debt/charge. Benefits are GRANTED as
-- rewards for activity (free_earn) or delivered over a participation term (noupfront_tier1); stopping forfeits
-- only the undelivered remainder — never a charge. data: {
--   user_id, mode (free_earn|noupfront_tier1), unlock_level (0..4), metric, activity_progress,
--   perks_granted, survey_earn_share_pct, term_years, active_window_days, started_at, last_active_at,
--   commitment_accepted, disclosures_version, owed (always 0) }
CREATE TABLE IF NOT EXISTS "EarnedAdvertiser" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "EarnedAdvertiser_data_gin" ON "EarnedAdvertiser" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "EarnedAdvertiser_created" ON "EarnedAdvertiser" (created_date DESC);

-- ReferralInviteBatch: DATA-MINIMIZED log of a user sending referral invites from their OWN device (native
-- SMS). Stores ONLY: who sent, the day, how many, the channel, whether the template was customized, and a
-- consent reference. It NEVER stores the contacts, phone numbers, names, or message bodies — those stay on
-- the user's device. Used for the anti-spam daily cap, attribution, and a consent audit trail. data: {
--   user_id, day, count, channel, template_customized, consent_ref }
CREATE TABLE IF NOT EXISTS "ReferralInviteBatch" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralInviteBatch_data_gin" ON "ReferralInviteBatch" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralInviteBatch_created" ON "ReferralInviteBatch" (created_date DESC);

-- AdGridResponse
CREATE TABLE IF NOT EXISTS "AdGridResponse" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdGridResponse_data_gin" ON "AdGridResponse" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdGridResponse_created" ON "AdGridResponse" (created_date DESC);

-- AdGridSession
CREATE TABLE IF NOT EXISTS "AdGridSession" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdGridSession_data_gin" ON "AdGridSession" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdGridSession_created" ON "AdGridSession" (created_date DESC);

-- AdGridSlotGrant: a one-day pass giving a high-value NON-premium user premium-priority AdGrid access,
-- reallocated from a premium member who didn't use their slot that day. data: { user_id, granted_date,
-- source, expires_at, used }
CREATE TABLE IF NOT EXISTS "AdGridSlotGrant" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdGridSlotGrant_data_gin" ON "AdGridSlotGrant" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdGridSlotGrant_created" ON "AdGridSlotGrant" (created_date DESC);

-- ScreenOutEvent: a survey disqualification (screen-out). Powers the reserve-gated consolation credit and
-- profiling analytics. data: { user_id, provider, survey_id, day, credited_usd }
CREATE TABLE IF NOT EXISTS "ScreenOutEvent" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ScreenOutEvent_data_gin" ON "ScreenOutEvent" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ScreenOutEvent_created" ON "ScreenOutEvent" (created_date DESC);

-- SurveyProfile: a user's "CYK" master profile — their real, stable demographic/screening answers, filled
-- once and kept current. Powers the provider profiler and CONFIRMED screening autofill ONLY. Never holds or
-- fills substantive survey content. data: { user_id, answers:{key:value}, updated_at }
CREATE TABLE IF NOT EXISTS "SurveyProfile" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SurveyProfile_data_gin" ON "SurveyProfile" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SurveyProfile_created" ON "SurveyProfile" (created_date DESC);

-- UserProductProfile
CREATE TABLE IF NOT EXISTS "UserProductProfile" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "UserProductProfile_data_gin" ON "UserProductProfile" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "UserProductProfile_created" ON "UserProductProfile" (created_date DESC);

-- Supplier
CREATE TABLE IF NOT EXISTS "Supplier" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Supplier_data_gin" ON "Supplier" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Supplier_created" ON "Supplier" (created_date DESC);

-- GiftCardStock
CREATE TABLE IF NOT EXISTS "GiftCardStock" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GiftCardStock_data_gin" ON "GiftCardStock" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GiftCardStock_created" ON "GiftCardStock" (created_date DESC);

-- GiftCardRedemption
CREATE TABLE IF NOT EXISTS "GiftCardRedemption" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GiftCardRedemption_data_gin" ON "GiftCardRedemption" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GiftCardRedemption_created" ON "GiftCardRedemption" (created_date DESC);

-- SourcedOrder
CREATE TABLE IF NOT EXISTS "SourcedOrder" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SourcedOrder_data_gin" ON "SourcedOrder" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SourcedOrder_created" ON "SourcedOrder" (created_date DESC);

-- BuyingDeskTask
CREATE TABLE IF NOT EXISTS "BuyingDeskTask" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "BuyingDeskTask_data_gin" ON "BuyingDeskTask" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "BuyingDeskTask_created" ON "BuyingDeskTask" (created_date DESC);

CREATE TABLE IF NOT EXISTS "BusinessAccount" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "BusinessAccount_data_gin" ON "BusinessAccount" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "BusinessAccount_created" ON "BusinessAccount" (created_date DESC);

CREATE TABLE IF NOT EXISTS "BusinessSubscription" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "BusinessSubscription_data_gin" ON "BusinessSubscription" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "BusinessSubscription_created" ON "BusinessSubscription" (created_date DESC);

CREATE TABLE IF NOT EXISTS "SponsoredPlacement" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SponsoredPlacement_data_gin" ON "SponsoredPlacement" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SponsoredPlacement_created" ON "SponsoredPlacement" (created_date DESC);

-- GoodsAdvance: optional 0% non-recourse in-store advance ledger (owner-scoped)
CREATE TABLE IF NOT EXISTS "GoodsAdvance" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GoodsAdvance_data_gin" ON "GoodsAdvance" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GoodsAdvance_created" ON "GoodsAdvance" (created_date DESC);

-- Tier1FinancedPlan: Tier 1 "pay-from-earnings" FINANCED package ledger (owner-scoped).
-- RECOURSE credit — the $12,000 is owed; earnings are swept toward it; remainder due at term end.
-- DISABLED by default and provider+counsel gated (see TIER1-FINANCED-PAY-FROM-EARNINGS.md).
CREATE TABLE IF NOT EXISTS "Tier1FinancedPlan" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Tier1FinancedPlan_data_gin" ON "Tier1FinancedPlan" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Tier1FinancedPlan_created" ON "Tier1FinancedPlan" (created_date DESC);

-- Tier1SelfPacedPlan: Tier 1 SELF-PACED advertising subscription ledger (owner-scoped).
-- NO DEBT — pay-as-you-go; benefits accrue in proportion to what's actually paid; nothing is ever owed.
-- Not credit, so it is ON by default and needs no provider/counsel gate (see TIER1-SELF-PACED-NO-DEBT.md).
CREATE TABLE IF NOT EXISTS "Tier1SelfPacedPlan" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Tier1SelfPacedPlan_data_gin" ON "Tier1SelfPacedPlan" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Tier1SelfPacedPlan_created" ON "Tier1SelfPacedPlan" (created_date DESC);

-- FunnelJourney: per-customer AI-funnel state (owner-scoped) — current product, gate, window start,
-- results, decision log. Drives Gate 1 (fit) and Gate 2 (results) recommendations. See AI-FUNNEL-DESIGN.md.
CREATE TABLE IF NOT EXISTS "FunnelJourney" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "FunnelJourney_data_gin" ON "FunnelJourney" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "FunnelJourney_created" ON "FunnelJourney" (created_date DESC);

-- FunnelEmailLog: audit + frequency-cap log for AI-concierge re-engagement emails (admin/global).
-- Sending is HARD-GATED by canEmailMarket() (consent) in funnelReengageEmail. See AI-FUNNEL-DESIGN.md.
CREATE TABLE IF NOT EXISTS "FunnelEmailLog" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "FunnelEmailLog_data_gin" ON "FunnelEmailLog" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "FunnelEmailLog_created" ON "FunnelEmailLog" (created_date DESC);

-- FunnelBenchmark: history/audit of AI-compiled "results" benchmarks per product (admin/global). Each row is
-- a real, substantiated figure (median/avg of N customers over their window) with its basis. See AI-FUNNEL-DESIGN.md.
CREATE TABLE IF NOT EXISTS "FunnelBenchmark" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "FunnelBenchmark_data_gin" ON "FunnelBenchmark" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "FunnelBenchmark_created" ON "FunnelBenchmark" (created_date DESC);

-- FlexPayPlan: last-resort installment "flexible payment terms" ledger (owner-scoped). INSTALLMENT CREDIT —
-- DISABLED by default, provider+counsel gated. Next-tier upsell is opt-in only, never a condition.
-- See FLEXIBLE-PAYMENT-TERMS-COMPLIANCE.md.
CREATE TABLE IF NOT EXISTS "FlexPayPlan" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "FlexPayPlan_data_gin" ON "FlexPayPlan" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "FlexPayPlan_created" ON "FlexPayPlan" (created_date DESC);

-- Tier2ScalingPlan: pay-as-you-go Tier 2 "Scale" progression (owner-scoped). NOT credit — each part is a
-- separate upfront purchase; this row tracks how many 30-day parts are done and when the current one started.
-- See TIER2-SCALING-OFFER.md.
CREATE TABLE IF NOT EXISTS "Tier2ScalingPlan" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Tier2ScalingPlan_data_gin" ON "Tier2ScalingPlan" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Tier2ScalingPlan_created" ON "Tier2ScalingPlan" (created_date DESC);

-- ProductStat: AI-compiled statistics on ANY product sold (admin/global). One row per product, refreshed by
-- productStatsCompile; published=true once the sample passes the threshold. See PRODUCT-STATS.md.
CREATE TABLE IF NOT EXISTS "ProductStat" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ProductStat_data_gin" ON "ProductStat" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ProductStat_created" ON "ProductStat" (created_date DESC);

-- AdvertiserApplication: lead capture from the public /Apply page (Founding advertiser + "coming soon"
-- financing interest). Marketing/CRM only — captures interest, NEVER originates credit. See Apply page.
CREATE TABLE IF NOT EXISTS "AdvertiserApplication" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdvertiserApplication_data_gin" ON "AdvertiserApplication" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdvertiserApplication_created" ON "AdvertiserApplication" (created_date DESC);

-- SaveToGetGoal: "Save-to-Get" item savings goal (owner-scoped). NO DEBT — the user reserves their OWN
-- Site Cash toward an item and claims it when funded; canceling returns the savings. Not credit. See SAVE-TO-GET.md.
CREATE TABLE IF NOT EXISTS "SaveToGetGoal" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SaveToGetGoal_data_gin" ON "SaveToGetGoal" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SaveToGetGoal_created" ON "SaveToGetGoal" (created_date DESC);

-- GiftBoost: user-triggered, PLATFORM-funded, non-cashable boost (owner-scoped by sender). Value flows
-- platform → recipient only (no wallet-to-wallet transfer), so it is not money transmission. See GIFT-BOOST.md.
CREATE TABLE IF NOT EXISTS "GiftBoost" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "GiftBoost_data_gin" ON "GiftBoost" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "GiftBoost_created" ON "GiftBoost" (created_date DESC);

-- PremiumBoostFunding: advertiser contributions to the premium-member boost pool (global). Each row is one
-- advertiser's funded amount; remaining_usd is drawn down as members claim. Funds the gift boost. See PREMIUM-GIFT-BOOST.md.
CREATE TABLE IF NOT EXISTS "PremiumBoostFunding" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PremiumBoostFunding_data_gin" ON "PremiumBoostFunding" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PremiumBoostFunding_created" ON "PremiumBoostFunding" (created_date DESC);

-- PremiumBoostGrant: per-member record of advertiser-funded boost claimed/used (owner-scoped by member_id).
CREATE TABLE IF NOT EXISTS "PremiumBoostGrant" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "PremiumBoostGrant_data_gin" ON "PremiumBoostGrant" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "PremiumBoostGrant_created" ON "PremiumBoostGrant" (created_date DESC);

-- AdvertiserReport: a stored weekly AI advertiser performance report (advertiserWeeklyReport). data:
-- { advertiser_id, window_days, generated_at, metrics, benchmarks, comparison, summary, recommendations,
--   substantiated, disclaimer }
CREATE TABLE IF NOT EXISTS "AdvertiserReport" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdvertiserReport_data_gin" ON "AdvertiserReport" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdvertiserReport_created" ON "AdvertiserReport" (created_date DESC);

-- AdvertiserMakeGood: a granted delivery make-good (deliveryMakeGoodSweep) — a free-inventory top-up owed on a
-- term-end delivery shortfall. data: { advertiser_id, seat_id, tier, guaranteed_units, delivered_at_grant,
-- shortfall_units, target_impressions, make_good_active, status, granted_at, expires_at, closed_at,
-- delivered_final, basis }
CREATE TABLE IF NOT EXISTS "AdvertiserMakeGood" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdvertiserMakeGood_data_gin" ON "AdvertiserMakeGood" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdvertiserMakeGood_created" ON "AdvertiserMakeGood" (created_date DESC);

-- AdvertiserReportedRevenue: off-platform revenue an advertiser connected/reported, counted (flagged) in the
-- performance metrics. data: { advertiser_id, amount_usd, source, period_start, period_end }
CREATE TABLE IF NOT EXISTS "AdvertiserReportedRevenue" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AdvertiserReportedRevenue_data_gin" ON "AdvertiserReportedRevenue" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AdvertiserReportedRevenue_created" ON "AdvertiserReportedRevenue" (created_date DESC);

-- AppLog: application/oversight log entries written by the oversight SDK. data: { level, source, message, meta }
CREATE TABLE IF NOT EXISTS "AppLog" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AppLog_data_gin" ON "AppLog" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AppLog_created" ON "AppLog" (created_date DESC);

-- DeviceToken: a registered push-notification device token for a user (registerPushToken). data:
-- { user_id, token, platform, last_seen }
CREATE TABLE IF NOT EXISTS "DeviceToken" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "DeviceToken_data_gin" ON "DeviceToken" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "DeviceToken_created" ON "DeviceToken" (created_date DESC);


-- ── Scale hot-path indexes (appended last, after every table exists) ─────────────────────────────────────
-- Append-heavy event/ledger tables are read time-ordered by the JSONB 'at' field. Without a btree on
-- (data->>'at'), those reads seq-scan + top-N sort (hundreds of ms at 300k, seconds at millions); the
-- expression index makes them index scans. Kept at the very end so all referenced tables are already created,
-- regardless of the order the table sections appear above. See tools/gen-schema.mjs.
CREATE INDEX IF NOT EXISTS "InteractionEvent_at" ON "InteractionEvent" ((data->>'at'));
CREATE INDEX IF NOT EXISTS "LiveMetricEvent_at" ON "LiveMetricEvent" ((data->>'at'));
CREATE INDEX IF NOT EXISTS "LoyaltyLedger_at" ON "LoyaltyLedger" ((data->>'at'));
CREATE INDEX IF NOT EXISTS "SessionCaptureFrame_at" ON "SessionCaptureFrame" ((data->>'at'));
CREATE INDEX IF NOT EXISTS "UXHeatmapSnapshot_at" ON "UXHeatmapSnapshot" ((data->>'at'));
CREATE INDEX IF NOT EXISTS "UserVariantState_at" ON "UserVariantState" ((data->>'at'));


-- CreativeAsset: AI Creative Suite generated ad variants + their live performance (see sdk/creative-suite.ts)
CREATE TABLE IF NOT EXISTS "CreativeAsset" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "CreativeAsset_data_gin" ON "CreativeAsset" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "CreativeAsset_created" ON "CreativeAsset" (created_date DESC);


-- SurveyDraft: AI Survey Suite generated surveys/drafts (see sdk/survey-suite.ts)
CREATE TABLE IF NOT EXISTS "SurveyDraft" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SurveyDraft_data_gin" ON "SurveyDraft" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SurveyDraft_created" ON "SurveyDraft" (created_date DESC);


-- SocialAmplificationEvent: a member posted an advertiser's AI social ad; carries reach -> est impressions ->
-- $ value that adds to the advertiser's delivered ad value (see sdk/social-amplification.ts)
CREATE TABLE IF NOT EXISTS "SocialAmplificationEvent" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "SocialAmplificationEvent_data_gin" ON "SocialAmplificationEvent" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "SocialAmplificationEvent_created" ON "SocialAmplificationEvent" (created_date DESC);


-- TierProgressionEvent: renewal offers, advance notices/applies, completions (see sdk/tier-progression.ts)
CREATE TABLE IF NOT EXISTS "TierProgressionEvent" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "TierProgressionEvent_data_gin" ON "TierProgressionEvent" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "TierProgressionEvent_created" ON "TierProgressionEvent" (created_date DESC);


-- VideoConcept: one sampled short-video concept from the AI Video Engine — its dimension tags (attributes),
-- attached live trend, compliance-safe brief, 0-100 predictive score, render phase, and (once tested) the
-- real measured metrics + blended performance + signed outcome weight (see sdk/video-engine.ts).
CREATE TABLE IF NOT EXISTS "VideoConcept" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "VideoConcept_data_gin" ON "VideoConcept" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "VideoConcept_created" ON "VideoConcept" (created_date DESC);
CREATE INDEX IF NOT EXISTS "VideoConcept_phase" ON "VideoConcept" ((data->>'phase'));
CREATE INDEX IF NOT EXISTS "VideoConcept_day" ON "VideoConcept" ((data->>'day'));
CREATE INDEX IF NOT EXISTS "VideoConcept_score" ON "VideoConcept" (((data->>'predictive_score')::float));


-- VideoTrend: the live trend pool (trending topics / current events) that grounds concepts for news-jacking
-- (see sdk/video-engine.ts, aiVideoEngineRefreshTrends).
CREATE TABLE IF NOT EXISTS "VideoTrend" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "VideoTrend_data_gin" ON "VideoTrend" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "VideoTrend_created" ON "VideoTrend" (created_date DESC);


-- ConceptPoll: a user poll built from generated video concepts — its concept pool + balanced matchups
-- (head-to-head / MaxDiff) + status (see sdk/concept-polling.ts).
CREATE TABLE IF NOT EXISTS "ConceptPoll" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ConceptPoll_data_gin" ON "ConceptPoll" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ConceptPoll_created" ON "ConceptPoll" (created_date DESC);
CREATE INDEX IF NOT EXISTS "ConceptPoll_status" ON "ConceptPoll" ((data->>'status'));


-- ConceptPollVote: one user's head-to-head / MaxDiff vote on a matchup (best + optional worst).
CREATE TABLE IF NOT EXISTS "ConceptPollVote" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ConceptPollVote_data_gin" ON "ConceptPollVote" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ConceptPollVote_created" ON "ConceptPollVote" (created_date DESC);
CREATE INDEX IF NOT EXISTS "ConceptPollVote_poll" ON "ConceptPollVote" ((data->>'poll_id'));
CREATE INDEX IF NOT EXISTS "ConceptPollVote_poll_user" ON "ConceptPollVote" ((data->>'poll_id'), (data->>'user_id'));


-- VideoPipelineRun: one end-to-end AI Video Autopilot run — its stage, poll, selected render candidates, the
-- human (or auto) decision, and trust bookkeeping for graduated autonomy (see sdk/video-autopilot.ts).
CREATE TABLE IF NOT EXISTS "VideoPipelineRun" (
  id           text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(),
  updated_date timestamptz NOT NULL DEFAULT now(),
  created_by   text,
  data         jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "VideoPipelineRun_data_gin" ON "VideoPipelineRun" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "VideoPipelineRun_created" ON "VideoPipelineRun" (created_date DESC);
CREATE INDEX IF NOT EXISTS "VideoPipelineRun_stage" ON "VideoPipelineRun" ((data->>'stage'));


-- Automation platform tables (see sdk/autonomy-kernel.ts, sdk/feedback.ts, sdk/fair-choice.ts):
-- FeedbackEvent: one standard feedback signal from any surface (explicit + implicit/auto-collected).
CREATE TABLE IF NOT EXISTS "FeedbackEvent" (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(), updated_date timestamptz NOT NULL DEFAULT now(),
  created_by text, data jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "FeedbackEvent_data_gin" ON "FeedbackEvent" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "FeedbackEvent_created" ON "FeedbackEvent" (created_date DESC);
CREATE INDEX IF NOT EXISTS "FeedbackEvent_domain" ON "FeedbackEvent" ((data->>'domain'));
CREATE INDEX IF NOT EXISTS "FeedbackEvent_surface" ON "FeedbackEvent" ((data->>'surface'));

-- AutonomyDecision: one gated decision for any domain + its human/auto outcome (trust history).
CREATE TABLE IF NOT EXISTS "AutonomyDecision" (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(), updated_date timestamptz NOT NULL DEFAULT now(),
  created_by text, data jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AutonomyDecision_data_gin" ON "AutonomyDecision" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AutonomyDecision_created" ON "AutonomyDecision" (created_date DESC);
CREATE INDEX IF NOT EXISTS "AutonomyDecision_domain" ON "AutonomyDecision" ((data->>'domain'));
CREATE INDEX IF NOT EXISTS "AutonomyDecision_stage" ON "AutonomyDecision" ((data->>'stage'));

-- AutonomyDomain: per-domain autonomy mode overrides set from the Command Center.
CREATE TABLE IF NOT EXISTS "AutonomyDomain" (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(), updated_date timestamptz NOT NULL DEFAULT now(),
  created_by text, data jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AutonomyDomain_data_gin" ON "AutonomyDomain" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AutonomyDomain_created" ON "AutonomyDomain" (created_date DESC);
CREATE INDEX IF NOT EXISTS "AutonomyDomain_did" ON "AutonomyDomain" ((data->>'domain_id'));

-- AutoCollectState: the cursor for the automatic telemetry→feedback miner (idempotency).
CREATE TABLE IF NOT EXISTS "AutoCollectState" (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(), updated_date timestamptz NOT NULL DEFAULT now(),
  created_by text, data jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "AutoCollectState_data_gin" ON "AutoCollectState" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "AutoCollectState_created" ON "AutoCollectState" (created_date DESC);

-- TrendChoiceEvent: impressions + picks for the unbiased current-event topic/ad chooser (fair scoring).
CREATE TABLE IF NOT EXISTS "TrendChoiceEvent" (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(), updated_date timestamptz NOT NULL DEFAULT now(),
  created_by text, data jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "TrendChoiceEvent_data_gin" ON "TrendChoiceEvent" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "TrendChoiceEvent_created" ON "TrendChoiceEvent" (created_date DESC);
CREATE INDEX IF NOT EXISTS "TrendChoiceEvent_topic" ON "TrendChoiceEvent" ((data->>'topic'));


-- ReferralBonus: a pending/paid two-tier referral bonus (Site Cash). $5 user / $2000 advertiser per tier;
-- the advertiser bonus pays only after cleared payment + clawback (see sdk/referral-tiers.ts).
CREATE TABLE IF NOT EXISTS "ReferralBonus" (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(), updated_date timestamptz NOT NULL DEFAULT now(),
  created_by text, data jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "ReferralBonus_data_gin" ON "ReferralBonus" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "ReferralBonus_created" ON "ReferralBonus" (created_date DESC);
CREATE INDEX IF NOT EXISTS "ReferralBonus_status" ON "ReferralBonus" ((data->>'status'));
CREATE INDEX IF NOT EXISTS "ReferralBonus_referrer" ON "ReferralBonus" ((data->>'referrer_user_id'));

-- EndorserConversion: a measured conversion from a member's disclosed sponsored post; the gated sweep pays a
-- Site Cash share of its value (see sdk/endorser-rewards.ts).
CREATE TABLE IF NOT EXISTS "EndorserConversion" (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(), updated_date timestamptz NOT NULL DEFAULT now(),
  created_by text, data jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "EndorserConversion_data_gin" ON "EndorserConversion" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "EndorserConversion_created" ON "EndorserConversion" (created_date DESC);
CREATE INDEX IF NOT EXISTS "EndorserConversion_status" ON "EndorserConversion" ((data->>'status'));
CREATE INDEX IF NOT EXISTS "EndorserConversion_member" ON "EndorserConversion" ((data->>'member_id'));

-- Advance: a FREE, NON-RECOURSE purchasing-power advance of store credit (see sdk/advance.ts). Recouped only
-- from the member's future advertiser-funded rewards; any shortfall is forgiven. Never a debt.
CREATE TABLE IF NOT EXISTS "Advance" (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  created_date timestamptz NOT NULL DEFAULT now(), updated_date timestamptz NOT NULL DEFAULT now(),
  created_by text, data jsonb NOT NULL DEFAULT '{}'::jsonb
);
CREATE INDEX IF NOT EXISTS "Advance_data_gin" ON "Advance" USING gin (data jsonb_path_ops);
CREATE INDEX IF NOT EXISTS "Advance_created" ON "Advance" (created_date DESC);
CREATE INDEX IF NOT EXISTS "Advance_status" ON "Advance" ((data->>'status'));
CREATE INDEX IF NOT EXISTS "Advance_member" ON "Advance" ((data->>'member_id'));
